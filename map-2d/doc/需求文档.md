# map-2d 二维地图模块 · 需求文档

| 项 | 内容 |
|---|---|
| 模块标识 | `map-2d`（源码模块，可独立运行、可整目录交付） |
| 文档版本 | v3.1 |
| 日期 | 2026-09-12 |
| 定位 | 二维地图渲染与绘制的**基座**：宿主只给数据、调接口，不接触 MapLibre |
| 编号体系 | `M2-<域>-<序号>`；域：MAP（地图能力）/ CTRL（控件）/ BASE（底图）/ LAYER（图层）/ DRAW（绘制）/ API（对外接口）/ NFR（非功能性） |
| 状态 | **只有两种：✅ 已完成 ｜ ⏳ 待完成**（另有"部分完成"，见备注列；不设"期/阶段"划分） |
| 来源 | 项目（用户提出）｜ 评审（评审补充）｜ 模块（模块自身演进） |
| 配套文档 | [设计文档.md](./设计文档.md)（已实现设计 §1–§9 + 待实现能力的设计方案 §10）｜ [接口文档.md](./接口文档.md)（已实现接口 §1–§9 + 待实现接口设计 §10） |

---

## 0. 完成情况总览（截至 2026-09-12）

> 一句话：**93 条需求，已完成 85 条，待完成 8 条**（91.4%）。
> 每条的状态在 §2 各域表格里逐条标注；本节是"一眼看全局"的汇总。
> 想看"点一下就能验证"的版本：`npm run dev:host` → http://localhost:5180/ 右侧「功能验收台」
> （说明见 [验收台使用说明.md](./验收台使用说明.md)）。

### 0.1 分域覆盖

| 域 | 条目 | ✅ 已完成 | ⏳ 待完成 | 覆盖度 |
|---|---|---|---|---|
| M2-MAP 地图能力 | 10 | 8 | 2 | ████████░░ 80% |
| M2-CTRL 地图控件 | 15 | 15 | 0 | ██████████ 100% |
| M2-BASE 底图 | 13 | 13 | 0 | ██████████ 100% |
| M2-LAYER 图层与图元 | 6 | 6 | 0 | ██████████ 100% |
| M2-DRAW 绘制接口 | 18 | 18 | 0 | ██████████ 100% |
| M2-API 对外接口 | 17 | 17 | 0 | ██████████ 100% |
| M2-NFR 非功能性 | 14 | 8 | 6 | ██████░░░░ 57% |
| **合计** | **93** | **85** | **8** | **█████████░ 91%** |

**剩余集中区**：M2-NFR（性能与工程性 7 条）、M2-MAP（2 条）。

### 0.2 ✅ 已完成（61 条）

| 域 | 已完成条目 | 数 |
|---|---|---|
| M2-MAP 地图能力 | 01–07：拖拽平移、缩放交互、初始视角配置、视角命令、场景切换平滑移动、二维锁定、视口回写<br>08：军用网格与经纬网（含 UTM/MGRS 换算） | 8 |
| M2-CTRL 地图控件 | 01–08：控件按需显示（默认全不显示）、指北针、鼠标经纬度、缩放按钮、比例尺、图层面板、地图工具条、显示模式徽标<br>09：显示模式手动切换　10：量算（测距/测面/方位角）　11：地图内图例　12：图层排序与分组透明度<br>13：主题热切换（日间/夜间/高对比）　14：键盘操作（平移/缩放/Esc/Enter）　15：性能观测接口 | 15 |
| M2-BASE 底图 | 01–07：本地瓦片、在线样式、无瓦片兜底、缺口观感、瓦片精度上限、运行中切换、版权标注<br>08：瓦片包版本与完整性校验（清单 + 抽样探测）<br>09–13：多底图注册与清单、整体替换切换、切换后状态保持、切换通知、精度上限只作用本地底图 | 13 |
| M2-LAYER 图层与图元 | 01–06：11 组独立开关、数据驱动渲染、业务数据入口、坐标显式入口、增量更新、脉冲动画 | 6 |
| M2-DRAW 绘制接口 | 01：12 类图元全部实现（含航线、圆形/椭圆区域、目标区域、圈层）<br>02–07：接口形态、图元显隐、快照导入导出、公里半径、样式覆盖、底图切换重放<br>08：手绘交互　09：圈层图元（距离环/方位线/方位圈/九宫格）　10：目标聚合　11：标签避让与分级<br>12：图元编辑　13：图元事件　14：吸附与对齐　15：样式模板　16：国军标标绘符号库<br>17：时间轴回放　18：回放数据接口 | 18 |
| M2-API 对外接口 | 01–07：单一入口、自带类型、开关集中、数据契约、控件开关接口、精度上限接口、批量提交<br>08：视图状态序列化　09：图片导出<br>10–13：底图清单查询、底图切换、切换回调、运行时增删底图<br>14：接口文档与最小示例　15：接口稳定性约定　16：回放控制接口　17：第三方接入指南 | 17 |
| M2-NFR 非功能性 | 01–05：流畅性、渲染分区、缺资源降级、可移植性、可构建性<br>06：底图加载观感（深色占比 28.9% → 5.7%）　10：错误边界　14：渲染时机合并（批量 50 写入 → 1 渲染） | 8 |

### 0.3 ⏳ 待完成（32 条）

| 域 | 待完成条目 | 数 |
|---|---|---|
| M2-MAP 地图能力 | 09 多底图"整体替换"的对外语义收口｜10 瓦片源自动降级（判定口径待重做） | 2 |
| M2-CTRL 地图控件 | —（**15/15 全完成**） | 0 |
| M2-BASE 底图 | —（**13/13 全完成**） | 0 |
| M2-DRAW 绘制接口 | —（**18/18 全完成**） | 0 |
| M2-API 对外接口 | —（**17/17 全完成**） | 0 |
| M2-NFR 非功能性 | 07 图元容量基线纳入回归｜08 多实例隔离｜09 移动端手势一致性｜11 性能预算与基线维护｜12 资源占用上限｜13 大数据量降级 | 6 |

**下一批计划**：多实例隔离（M2-NFR-08，破坏性改动：地图单例改注册表）、资源占用上限（M2-NFR-12）、大数据量降级（M2-NFR-13）、性能基线与预算（M2-NFR-07/11）、移动端手势（M2-NFR-09），以及重做 M2-MAP-10 的降级判定与 M2-MAP-09 语义收口。

---

## 1. 模块概述

### 1.1 定位

本模块是**二维地图能力的基座**：其他模块（本项目主系统界面层，或任何第三方项目）不需要了解 MapLibre，只看《接口文档》就能把地图用起来——注入数据、调用接口，其余由本模块负责。

**设计原则**

| 原则 | 含义 |
|---|---|
| **性能优先** | 渲染性能与响应速度是首要指标；凡能提升性能的做法（图层/数据源策略、增量更新、缓存、懒加载、批次提交）都可采用，不因"改动大"而回避 |
| **接口完备** | 能力一律接口化：能被调用、能被查询、能被配置、能被关闭。凡是宿主可能需要的控制点，都必须有接口 |
| **文档先行** | 交付物含《接口文档》：看文档即可接入，无需读源码 |
| **独立自洽** | 模块代码不依赖任何业务后端；地图数据（瓦片）由宿主配置指向（本地瓦片服务或在线源），模块不内置瓦片 |
| **不夹带业务** | 不含业务逻辑、界面框架、数据获取 |

### 1.2 运行形态

| 形态 | 方式 | 说明 |
|---|---|---|
| 独立运行 | `npm run dev` → `http://localhost:5180/` | **模块自带的演示宿主**：自带示例图元、零业务后端依赖。用途是演示、调试、交付验证与性能基线测量。它的底图瓦片仍由配置指向；指向的服务不可用时回落纯色底 |
| 嵌入宿主 | `import { MapView, MapDraw } from '@map2d'` | 数据经 props 注入；宿主不出现 source/layer/样式概念 |

### 1.3 依赖与交付

| 项 | 内容 |
|---|---|
| 运行时依赖 | 仅 `maplibre-gl` / `react` / `zustand` |
| 交付形态 | 源码模块（**不发布 npm 包**，后续再议）；可整目录拷贝，可 `git subtree` 独立成仓 |
| 接口文档 | [接口文档.md](./接口文档.md)（接入的唯一依据） |
| 代码依赖边界 | 不依赖任何业务后端代码、宿主状态管理、宿主 UI 组件库 |
| 数据来源边界 | 瓦片由配置指向；**底图数据一律本地化**，不依赖联网在线源 |

### 1.4 范围边界

| 边界 | 内容 |
|---|---|
| 🚫 三维能力 | 地形、三维建筑、2D/3D 切换、楼层与室内 |
| 🚫 业务逻辑 | 阶段流程、目标台账、威胁评级、链路状态机 |
| 🚫 数据获取 | 业务数据一律由宿主注入（底图瓦片请求除外，那是渲染的必要条件） |
| 🚫 界面框架 | 左侧导航、业务面板、状态栏、顶栏 |
| 🚫 瓦片工程 | 切图、抓取、打包、分发 |
| 🚫 在线底图适配 | 不接国内在线底图源（天地图/高德等），不做坐标系偏移适配；底图一律本地化 |

---

## 2. 功能需求条目

### 2.1 地图能力（M2-MAP）

| 编号 | 需求描述 | 验收标准 | 状态 | 来源 |
|---|---|---|---|---|
| M2-MAP-01 | 拖拽平移：左键拖拽 / 触屏单指 | 连续拖拽不卡顿、不错位 | ✅ | 项目 |
| M2-MAP-02 | 缩放交互：滚轮以光标为中心、双击放大、触屏双指捏合 | 缩放中心正确、层级过渡无白屏 | ✅ | 项目 |
| M2-MAP-03 | 初始视角与缩放范围由宿主配置下发（center / zoom / minZoom / maxZoom） | 未配置时用模块内置默认且可正常出图 | ✅ | 项目 |
| M2-MAP-04 | 视角命令接口：设置视角、飞行动画、定位到坐标、复位视角、相对缩放、读取当前视口、查询就绪状态 | 各命令调用后视口变化符合预期；就绪查询可被宿主轮询 | ✅ | 项目 |
| M2-MAP-05 | 换场景/任务时平滑移动视角 | 视角切换有过渡动画、无跳变 | ✅ | 项目 |
| M2-MAP-06 | 二维锁定：禁旋转、禁俯仰，地图方向恒定（正北朝上） | 任何操作后 bearing 与 pitch 恒为 0 | ✅ | 项目 |
| M2-MAP-07 | 视口回写：当前中心经纬度与层级可供宿主读取 | 拖动后宿主读到的视口与画面一致 | ✅ | 项目 |
| M2-MAP-08 | 军用网格与经纬网：可叠加 MGRS 网格或经纬网（含 UTM 带号），并可与状态栏坐标联动 | `setGrid('graticule'|'utm'|'none')`；含 WGS84→UTM→**MGRS 军用格网参考**换算（自实现，无依赖）。实测：北京 116.3974/39.9093 → `50S 448494 4417864`、MGRS `50S DK 48494 17864`；上海 → `51R CQ 54633 56140`；经纬网按缩放选间隔（z12 生成 38 线 + 38 标注）、UTM 方格标注带格网首段（如 `50S 424`）；可开关且关闭后要素归零 | ✅ | 评审 |
| M2-MAP-09 | **多底图包切换与全体替换**：宿主声明的多套本地底图之间切换时，**整幅底图整体替换**（不是局部换层）；切换由底图模块统一处理 | 切换后整幅底图换为该套底图；无残留、无混层 | ⏳ | 项目 |
| M2-MAP-10 | 瓦片源自动降级：瓦片源不可用时按"本地 → 纯色兜底"顺序自动切换并提示 | ⏳ **未达成**：纯色兜底与"降级状态/事件/查询接口"已实现（`getTileState()`、`onTilesDegraded()`，指标 `degraded` 位已接真实状态），但**判定口径不成立**——实测发现 MapLibre 的 `sourcedata`（含带 `tile` 字段的）是**逐瓦片进度事件**，真实页面每秒触发上千次（9199 次/5 秒），会把"成功加载数"抬得极高，使降级条件 `成功数 < 3` 永远不成立。需改为**网络层挂钩**（包装 fetch/XHR 或在瓦片请求处埋点）重做判定 | 评审 |

> **说明**：原"坐标系适配（WGS84/GCJ-02/BD-09）"与"多底图叠加（影像+标注分层透明度）"两条，按本轮决策**取消**：
> 前者只在国内在线底图源场景下需要，本项目底图一律本地化；后者与"每套底图整体替换"的定位冲突（见 §5 决策记录）。

### 2.2 地图控件（M2-CTRL）

> M2-CTRL-01 ~ 05 来自需求补充 1：**控件能力默认具备，但默认不显示**，由宿主按需调用显示接口——如同 C++ 接口，不调用就不出现。

| 编号 | 需求描述 | 验收标准 | 状态 | 来源 |
|---|---|---|---|---|
| M2-CTRL-01 | 控件按需显示机制：地图控件（指北针、鼠标位置经纬度、缩放按钮、比例尺）**默认全部隐藏**，提供接口按需开启/关闭，可单独控制、可组合控制 | 未调用接口时地图上只有底图与绘制物；调用某控件接口后仅该控件出现，关闭后立即消失 | ✅ | 项目 |
| M2-CTRL-02 | 指北针：随地图方向旋转、点击复位正北；默认隐藏 | 开启后常显且指向正确；点击后方向复位 | ✅ | 项目 |
| M2-CTRL-03 | 鼠标位置经纬度：实时显示光标所在经纬度，随鼠标移动刷新；默认隐藏 | 开启后经纬度随鼠标移动更新，且与地图实际位置一致 | ✅ | 项目 |
| M2-CTRL-04 | 缩放按钮：`+ / −` 缩放控件，可放置于指定角落；默认隐藏 | 开启后按钮可用，点击后层级变化正确 | ✅ | 项目 |
| M2-CTRL-05 | 比例尺：公制比例尺，可指定单位与最大宽度；默认隐藏 | 开启后比例尺与实际比例一致，随缩放更新 | ✅ | 项目 |
| M2-CTRL-06 | 图层开关面板：11 个图层分组独立开关，开关状态跨底图切换保留 | 每组可独立开关；切底图后开关状态不丢 | ✅ | 项目 |
| M2-CTRL-07 | 地图工具条：选择 / 图层 / 清屏 / 全屏 | 各按钮生效；清屏只留地图与绘制物 | ✅ | 项目 |
| M2-CTRL-08 | 显示模式徽标：按场景 + 阶段展示 10 种显示模式取值 | 徽标取值随场景/阶段正确变化 | ✅ | 项目 |
| M2-CTRL-09 | 显示模式手动切换：支持宿主/用户手动指定显示模式（`setDisplayMode`），并给出优先级约定 | 实测优先级：**手动值优先于阶段推导**（阶段变化时只更新 `derived`，不动 `mode`）；`clearDisplayMode()` 回落推导值；**切换场景自动解除手动覆盖**（换任务不该沿用上个任务的模式）；`listDisplayModes()` 给出可选清单 | ✅ | 项目 |
| M2-CTRL-10 | 量算工具：测距（折线距离）、测面积、方位角与距离（起点→终点） | 实测口径：1° 纬度 = 111195 m；北京→上海 = 1068.2 km；方位角正东 = 90.0°；0.1°×0.1° 于 40°N = 94.7 km²（与手算一致）；结果可读可清 | ✅ | 评审 |
| M2-CTRL-11 | 图例：地图内图例（威胁等级、目标状态、链路状态、图元类型四组） | 图例内容取自图元真实配色来源；可开关（`controls.legend`，默认不显示） | ✅ | 评审 |
| M2-CTRL-12 | 图层排序与透明度：宿主可调整图层叠放顺序、设置分组整体透明度 | 调整后立即生效；顺序可查询（`getLayerOrder`）；透明度**保留图元自身语义**（区域 0.1 × 0.5 = 0.05） | ✅ | 评审 |
| M2-CTRL-13 | 主题热切换：日间 / 夜间 / 高对比三套配色与底图参数，运行中一键切换 | `applyTheme('day'|'night'|'contrast')`；每套含底图参数（亮度区间/饱和度/对比度/叠加色）与图元可读性（标签描边、文字色）。实测三套参数互不相同、**不重建样式不刷新页面**；**换底图/精度变更导致样式重建后主题自动重套**（否则会被重置为默认值） | ✅ | 评审 |
| M2-CTRL-14 | 键盘操作：方向键平移、`+/-` 缩放、Esc 取消当前绘制/选择、Enter 完成绘制、Backspace 退一点 | 快捷键生效且不干扰输入框（INPUT/TEXTAREA/SELECT 内不接管） | ✅ | 评审 |
| M2-CTRL-15 | 性能观测接口：可查询帧率、图元数量、瓦片缓存量等运行指标 | 宿主可读取指标用于自检与排障 | ✅ | 评审 |

### 2.3 底图（M2-BASE）

| 编号 | 需求描述 | 验收标准 | 状态 | 来源 |
|---|---|---|---|---|
| M2-BASE-01 | 本地栅格瓦片底图：按 `{z}/{x}/{y}` 模板加载本地托管瓦片 | 局域网内拖拽/缩放全程无外网请求 | ✅ | 项目 |
| M2-BASE-02 | 在线样式底图：支持直接下发样式 URL（矢量/栅格） | 配置样式 URL 后底图正确显示 | ✅ | 项目 |
| M2-BASE-03 | 无瓦片兜底：瓦片缺失或服务端未启动时页面仍可打开、可交互 | 断开瓦片服务后页面正常打开，控制台仅汇总告警 | ✅ | 项目 |
| M2-BASE-04 | 底图缺口观感：缺口用地面色而非纯黑，瓦片不做淡入 | 缺口处不出现刺眼纯黑 | ✅ | 项目 |
| M2-BASE-05 | **瓦片精度上限（按地面分辨率）**：提供接口限制底图**最多使用到指定地面分辨率**（米/像素），超过该精度的更细层级不再请求，改由已加载瓦片放大显示 | 设定上限后，放大到更细层级不再产生新的瓦片请求；画面为放大显示但保持连续 | ✅ | 项目 |
| M2-BASE-06 | 运行中切换底图（本地底图之间 / 本地瓦片 ↔ 样式）且已绘制图元不丢 | 切换后图元仍在原位置，无闪烁丢失 | ✅ | 项目 |
| M2-BASE-07 | 版权标注：机制保留、可开关；关闭时不写入样式署名 | 开关打开后右下角常显署名 | ✅ | 项目 |
| M2-BASE-08 | 瓦片包版本与完整性校验：可查询瓦片包版本、缺块清单，用于多机交付排障 | `scripts/gen-tile-manifest.mjs` 从瓦片包生成清单（版本/各层数量/键列表/sha256）；`validateTiles()` 做**清单校验 + 抽样 HEAD 探测**。实测：本地包清单 v1、15 级、14204 张，五项检查通过（清单可读/版本匹配/模板一致/层级连续/总数一致）；抽样 30 张发现 16 张不可读——**结论正确**（该包高精度仅覆盖北京与上海，抽样命中了无覆盖区）；清单缺失时降级为 `sampled` 状态并给出可读原因 | ✅ | 评审 |
| M2-BASE-09 | **多底图包注册与清单**：宿主可声明**多套已下载到本地的底图**（卫星影像、路网、地形等），每套含标识、名称、类型、服务地址或瓦片模板、层级范围、分辨率、版权署名等元数据，形成可枚举清单 | 传入多套底图描述后可枚举出完整清单；未声明时回落单底图默认行为 | ✅ | 项目 |
| M2-BASE-10 | **底图切换（整体替换）**：按底图标识切换到指定底图，**整幅底图整体替换**，不做局部混合 | 切换后整幅底图变化；无残留、无混层、无报错 | ✅ | 项目 |
| M2-BASE-11 | 切换后状态保持：切换底图后保持当前视角、已绘制图元不丢、图层分组开关与控件开关状态保留 | 切换前后对比：视角一致、图元位置一致、开关状态一致 | ✅ | 项目 |
| M2-BASE-12 | 底图切换通知：切换完成后向宿主发出通知（携带当前底图标识），便于宿主同步界面上的底图选择状态 | 宿主可收到通知并把界面对齐到当前底图 | ✅ | 项目 |
| M2-BASE-13 | 精度上限只作用于本地底图：在线样式底图不受精度上限约束（在线卡顿属在线源自身问题） | 设定上限后本地栅格源 maxzoom 受限；切到在线样式后**样式内已无本地栅格源**，模块不再参与其层级 | ✅ | 项目 |

### 2.4 图层与图元（M2-LAYER）

| 编号 | 需求描述 | 验收标准 | 状态 | 来源 |
|---|---|---|---|---|
| M2-LAYER-01 | 图层分组与独立开关：区域、链路、集群、目标、无人机、扫描、轨迹、尾迹、脉冲、标注、航线/图形区共 11 组 | 每组独立开关、叠加正确 | ✅ | 项目 |
| M2-LAYER-02 | 数据驱动渲染：颜色/标签/选中/状态/威胁级别由图元属性决定，未给则取内置调色板 | 同一份数据换配色无需改模块代码 | ✅ | 项目 |
| M2-LAYER-03 | 业务数据入口：目标、编组、无人机、链路、轨迹按数组批量灌入 | 数据变化后地图增量更新、无重建抖动 | ✅ | 项目 |
| M2-LAYER-04 | 坐标显式入口：区域、扫描、脉冲、标注、链路、编组、目标、无人机、轨迹按坐标数组灌入 | 宿主可完全自绘而不依赖业务数据结构 | ✅ | 项目 |
| M2-LAYER-05 | 增量更新：数据变更走数据源 `setData`，不重建图层 | 高频更新（≥1 Hz）无卡顿、无闪烁 | ✅ | 项目 |
| M2-LAYER-06 | 脉冲扩散动画：目标/无人机外围扩散环 | 动画连续、缩放后半径仍正确 | ✅ | 项目 |

### 2.5 绘制接口（M2-DRAW）

> M2-DRAW-01 ~ 03 来自需求补充 2：绘制能力以**接口**形式提供，覆盖常见可绘制对象，并支持**显隐（显影）**控制。

| 编号 | 需求描述 | 验收标准 | 状态 | 来源 |
|---|---|---|---|---|
| M2-DRAW-01 | 绘制对象清单（接口化）：**无人机**、**无人机航线**、**无人机轨迹**、**不规则区域**、**封闭区域**、**圆形/椭圆形区域**、**目标区域**、**目标**、集群编组、扫描覆盖、脉冲标记、文本标注 | 每类都有新增/更新/删除接口，且能正确呈现 | ✅ **11 类全部实现**（新增：航线 oute、圆形/椭圆/目标区域 shape） | 项目 |
| M2-DRAW-02 | 图元接口形态：整组替换、单个新增/更新、单个删除、按类清空、全部清空、读取列表 | 接口齐全、语义清晰，宿主无需了解内部存储 | ✅ | 项目 |
| M2-DRAW-03 | **图元显隐（显影）**：每个图元可单独设置显示/隐藏，并可整类或全部显隐 | 隐藏后该图元不显示但数据保留；重新显示立即恢复 | ✅ | 项目 |
| M2-DRAW-04 | 图元快照：一次导出全部图元、一次导入覆盖（可 JSON 序列化） | 导出→导入后画面一致 | ✅ | 项目 |
| M2-DRAW-05 | 地理尺寸表达：扫描/圆形区域的半径用"公里"表达并按当前缩放换算 | 缩放后半径对应的实地尺寸不变 | ✅ | 项目 |
| M2-DRAW-06 | 样式可覆盖：颜色、透明度、虚线、标签、选中态等逐图元可指定 | 逐图元样式生效且互不影响 | ✅ | 项目 |
| M2-DRAW-07 | 重放能力：底图样式重建后已绘制图元自动重放 | 切换底图后图元不丢 | ✅ | 项目 |
| M2-DRAW-08 | 手绘交互：鼠标/触屏绘制点、线、面（测距、区域绘制、新建、标绘）；与图元编辑**同期实现** | 绘制结果进入图元集合并可按 M2-DRAW-04 导出；**绘制中为预览、不进集合**，双击/Enter 完成即落库 | ✅ | 项目 |
| M2-DRAW-09 | 圈层类图元：距离环（等距圈）、方位线（射线）、方位圈、九宫格/参考线 | 新增图元类型 `annulus`（kind: ring/bearing-ring/bearing-line/grid）；实测距离环首圈半径 5.00km、方位线 90° 为正东（纬度不变、经度增加）、九宫格 3×3 生成 8 条参考线；图元可命中、可显隐、可导出 | ✅ | 评审 |
| M2-DRAW-10 | 目标聚合：密集目标按缩放聚合为计数气泡，放大自动散开、点击可钻取 | 按**屏幕像素**聚类（默认 42px，随缩放自然变化，无需维护每级阈值表）；实测 60 个密集目标在 z12 聚成 1 个气泡、放大到 z15（超过 maxZoom）恢复 60 个独立点；统计口径 `getClusterStats()` 可查（原始数/聚合后数/是否在聚合） | ✅ | 评审 |
| M2-DRAW-11 | 标签避让与分级显示：标签重叠自动错让；按缩放级别配置显示哪一级标签 | 避让用渲染器内建碰撞检测（`text-allow-overlap:false` + `text-ignore-placement:false`，两层标签层均开启）；分级按**逐图元 `minZoom`** 生效（不需要先启用策略）——实测 z10 渲染 1 个、z13 渲染 2 个、z16 渲染 3 个；另有全图数量上限策略 `setLabelPolicy({maxLabels})` | ✅ | 评审 |
| M2-DRAW-12 | 图元编辑：顶点拖拽、插入/删除顶点（与手绘**同期实现**）；整体拖拽经"顶点全选"等价实现 | 编辑结果可回读（`list` 反映最新坐标）；顶点数有下限保护（区域≥3、折线≥2） | ✅ | 项目 |
| M2-DRAW-13 | 图元事件回调：点击 / 悬停（选中与拖拽结束待手绘/编辑一并提供） | 宿主可据此联动业务面板；点击空白不误触发；**11 类图元逐一验证可命中** | ✅（点击/悬停） | 评审 |
| M2-DRAW-14 | 吸附与对齐：顶点吸附到已有图元顶点（屏幕 10px 阈值换算）、绘制时实时显示长度/面积 | 靠近既有顶点时自动吸附并给出提示（吸附来源标注 kind:id） | ✅ | 评审 |
| M2-DRAW-15 | 样式模板：命名样式（颜色/线型/字号/图标）可复用，图元可引用模板名 | `setStyleTemplates({名字: {color,dashed,weight,size,opacity}})` + 图元写 `style: '名字'`；**改模板即批量生效**（图元存的是名字，渲染时才解析）；图元自身字段优先级更高（实测覆盖项不被模板改动影响）；未登记的名字回落图元字段+内置调色板并提示一次 | ✅ | 评审 |
| M2-DRAW-16 | **军事标绘符号库（国军标）**：按国军标（GJB）符号体系绘制单位符号，支持旋转与缩放 | 图元类型 `symbol`：内置 **12 个常用兵种符号**（步兵/装甲/炮兵/导弹/雷达/指挥所/侦察/电子对抗/后勤/卫生/航空兵/无人机），**框形与颜色随敌我属性变化**（我方=矩形+蓝、敌方=菱形+红、中立=方块+绿、不明=圆角+黄）；支持 `rotation` 旋转与 `size` 缩放；`registerSymbol()` 可扩展自定义符号。符号图像由模块**同步逐像素栅格化**（不依赖图标字体或外部图片） | ✅ | 项目 |
| M2-DRAW-17 | **时间维度回放（模块提供时间轴）**：模块内置时间轴与播放控制（播放/暂停/倍速/拖拽定位/自动跟随），宿主提供带时间戳的轨迹数据与存储；模块负责按当前时刻截取并渲染 | `<ReplayBar />` 提供播放/暂停/上一下一采样点/拖拽定位/倍速(0.5–8×)/自动跟随；实测 4× 播 1.2 秒推进 4750 ms，暂停后时刻冻结；跟随开启时相机随对象移动 | ✅ | 项目 |
| M2-DRAW-18 | **回放数据接口**：回放所需数据（带时间戳的轨迹点集）由宿主提供，模块提供加载/替换/清空接口，**不负责数据存储与持久化** | `loadReplay()` / `clearReplay()`；实测清空后时间轴消失、地图图元归零；采样点按时间自动排序；每条轨迹可独立设定生效时间范围（目标 15 秒后才出现，之前不显示） | ✅ | 项目 |

### 2.6 对外接口（M2-API）

| 编号 | 需求描述 | 验收标准 | 状态 | 来源 |
|---|---|---|---|---|
| M2-API-01 | 单一公开入口：宿主只从一个入口导入（地图容器、工具条、图层面板、指北针、徽标、绘制接口、视角命令、UI 状态、开关） | 宿主代码中不出现模块内部路径 | ✅ | 项目 |
| M2-API-02 | 自带类型契约：对外类型全在模块内定义，宿主结构兼容即可直接传入 | 宿主不引用模块类型也能编译通过 | ✅ | 项目 |
| M2-API-03 | 模块开关集中：版权署名、瓦片淡入等集中一处可调 | 改一个开关即生效 | ✅ | 项目 |
| M2-API-04 | 数据注入契约：地图容器只接受一个数据对象（配置 + 场景/阶段 + 目标/编组/无人机/链路/轨迹） | 缺字段时按约定降级，不报错 | ✅ | 项目 |
| M2-API-05 | 控件按需显示接口（对应 M2-CTRL-01）：提供开启/关闭各控件的统一入口 | 调用即显示、不调用不显示 | ✅ | 项目 |
| M2-API-06 | 瓦片精度上限接口（对应 M2-BASE-05）：按地面分辨率（米/像素）设定上限（也接受等效层级） | 传参后仅使用不细于该分辨率的瓦片 | ✅ | 项目 |
| M2-API-07 | 图元批量接口：一次提交大批量图元（新增/更新/删除混合），内部批量提交、单帧渲染 | 大批量提交不产生多次重绘 | ✅ | 评审 |
| M2-API-08 | 视图状态序列化：当前视角（中心/层级/显示模式/图层开关/控件开关/当前底图/精度上限/图元）可导出与恢复 | 实测：导出 → 打乱（换视角/关控件/改透明度/清图元）→ 恢复后六项（视角/控件/透明度/分组开关/图元数/底图）**逐项一致** | ✅ | 评审 |
| M2-API-09 | 图片导出：把当前地图（含图元与已开启控件）导出为图片（PNG/JPEG、可选是否带控件） | 实测：PNG 1582×798（约 2.9 MB）与画布同尺寸；JPEG(0.8) 约 321 KB；`downloadImage()` 可直接下载 | ✅ | 评审 |
| M2-API-10 | 底图清单查询接口：返回全部可用底图（标识、名称、类型、分辨率、层级范围、是否当前） | 调用即可拿到可枚举清单，供宿主渲染底图下拉/切换按钮 | ✅ | 项目 |
| M2-API-11 | 底图切换接口：按标识切换底图；未找到标识时返回明确失败原因而不抛异常 | 有效标识切换成功；无效标识地图保持原状并有可读错误 | ✅ | 项目 |
| M2-API-12 | 底图切换回调：注册切换后回调（携带底图标识与元数据） | 切换后回调被触发一次，参数与当前底图一致 | ✅ | 项目 |
| M2-API-13 | 运行时注册/注销底图：除初始化时声明外，也支持运行中追加或移除底图包 | 追加后立即出现在清单里并可切换；移除当前底图时有明确约定行为 | ✅ | 项目 |
| M2-API-14 | 接口文档与最小示例：对外能力有完整接口文档（签名/参数/默认值/示例/错误约定），并附可运行的最小示例 | 接入方只看文档即可完成接入；`examples/minimal.tsx` 为零后端依赖的最小示例（含常用操作速查） | ✅ | 项目 |
| M2-API-15 | 接口稳定性约定：明确对外接口的破坏性变更规则（废弃流程、兼容期），并提供一页"变更记录" | `doc/接口稳定性约定.md`：对外接口范围界定、变更分级（Patch/Minor/Major）、废弃过渡期（至少一个次版本）、稳定但不承诺清单、变更记录表、宿主升级自查清单 | ✅ | 项目 |
| M2-API-16 | **回放控制接口**（对应 M2-DRAW-17/18）：加载/替换/清空回放数据，播放/暂停/倍速/定位到时刻/查询当前时刻 | 共 11 个接口（loadReplay/clearReplay/playReplay/pauseReplay/toggleReplay/setReplaySpeed/seekReplay/seekReplayProgress/stepReplay/setReplayFollow/getReplayStatus/onReplayChange）；位置按时间线性插值，航向与速度可由相邻点自动推算 | ✅ | 项目 |
| M2-API-17 | **第三方接入指南**：面向"拷贝过去用"的接入方，写明需要拷贝什么、必须配置什么、可选能力如何开启 | `doc/第三方接入指南.md`：三条接入路线、必拷/不必拷清单、subtree 命令、依赖与别名、瓦片三种托管方式、最小数据契约、宿主禁止清单、30 分钟跑通路径与排障顺序 | ✅ | 项目 |

### 2.7 非功能性需求（M2-NFR）

> M2-NFR-07 的指标以本机实测基线为准（§4 性能基线），非拍脑袋数字。

| 编号 | 需求描述 | 验收标准 | 状态 | 来源 |
|---|---|---|---|---|
| M2-NFR-01 | 流畅性：拖拽、缩放、图元高频更新无明显卡顿 | 连续操作 30 秒帧率稳定 | ✅ | 项目 |
| M2-NFR-02 | 渲染分区：底图与图元走 WebGL，宿主 DOM 面板不受地图刷新影响 | 图元更新时界面面板不重排 | ✅ | 项目 |
| M2-NFR-03 | 缺资源降级：瓦片失败不隐藏图层、不中断交互 | 单张缺失不影响整张底图 | ✅ | 项目 |
| M2-NFR-04 | 可移植性：整目录拷贝到新项目即可编译运行 | 新项目 `npm install && npm run dev` 出图 | ✅ | 项目 |
| M2-NFR-05 | 可构建性：类型检查与构建通过，产物可预览 | `npm run typecheck` / `npm run build` 均通过 | ✅ | 项目 |
| M2-NFR-06 | 观感优化（遗留）：缩放/拖拽过程中不出现整屏深蓝底色 | ✅ **已解决**。原根因：底图被主题压暗到亮度上限 0.42，「正常的暗」与「缺口底色 #16283a」在视觉上分不清，看起来就是整屏深蓝。改为 0.72 后实测：**深色像素占比 28.9% → 5.7%、平均亮度 55.5 → 79.2**；连续快速缩放（±1.2 级 × 20 次）期间深色占比仅波动 **+0.3pp**（修复前 +2.3pp）。注：曾试过的"启动预热 + 缩放顶底层"方案确实会变卡，已回退，本轮走的是**配色与亮度**路线，不增加任何请求 | 项目 |
| M2-NFR-07 | **图元容量基线（性能优先的量化门槛）**：以 §4 实测基线为验收口径——单次提交耗时与单点更新耗时不得超过基线，超基线即视为劣化 | 复现基准脚本，各项耗时不超过基线值（同一机器口径） | ⏳ | 项目 |
| M2-NFR-08 | 多实例隔离：同一页面可存在多个地图实例且互不干扰（**纳入待完成，不做"期"的划分**） | 两实例各自的图层、控件、图元、视角互不影响 | ⏳ | 项目 |
| M2-NFR-09 | 移动端手势一致性：平板/触屏上双指缩放、单指拖动、长按选择、双击缩放均可用 | 主流平板浏览器实测通过 | ⏳ | 评审 |
| M2-NFR-10 | 错误边界：单个图元数据非法不拖垮整图渲染；异常可定位到具体图元 | 注入一条脏数据后其余图元照常显示 | ✅ | 评审 |
| M2-NFR-11 | 性能预算与基线：为关键路径设定可测量指标（首屏可交互时间、连续帧率、批量提交耗时、内存占用），并维护基线数据 | 给出基线数字，回归时可对比"未劣化" | ⏳ | 项目 |
| M2-NFR-12 | 资源占用上限：明确瓦片缓存、图元数据、解码纹理的内存上限与淘汰策略，长时间运行不持续增长 | 连续运行 30 分钟内存无单调增长 | ⏳ | 项目 |
| M2-NFR-13 | 大数据量渲染策略：图元数量超过基线区间时按需降级（抽稀、聚合、简化几何）而非卡死或丢帧 | 超阈值时仍可交互，且明确告知已降级 | ⏳ | 项目 |
| M2-NFR-14 | 渲染时机控制：支持合并同一帧内的多次数据更新（批量提交、单帧渲染），避免高频更新引发重复绘制 | 实测：`batch()` 内 50 次写入 → **1 次渲染**；非批量 50 次写入 → 50 次渲染（1:1，语义明确）。指标可查：`getStats().render = {writes, renders, coalesced, writesPerSec, rendersPerSec}` | ✅ | 项目 |

---

## 3. 验收准则

**功能验收**

1. 独立运行 `npm run dev` 打开即出图（底图瓦片由配置指向）；
2. 地图漫游、缩放、二维锁定、视角命令均按 M2-MAP 各条通过；
3. 默认状态下不显示任何地图控件；按 M2-CTRL-01 / M2-API-05 逐个开启后，指北针、经纬度、缩放按钮、比例尺、图例分别正确出现并可关闭（**且互不遮挡**，四角分工见《设计文档》§7.1）；
4. 11 个图层分组可独立开关，状态跨底图切换保留；图层叠放次序可调、分组整体透明度可调（M2-CTRL-12）；
5. 绘制接口覆盖 M2-DRAW-01 清单中的全部对象（**13 类**：含航线、圆形/椭圆、目标区域、圈层、国军标标绘符号），且每个图元可按 M2-DRAW-03 单独显隐；
6. 手绘与编辑（M2-DRAW-08 / 12）在同一批交付内可用：工具条"测距/区域/新建/标绘"与独立宿主"绘制"按钮组均可进入绘制；双击/Enter 完成、Esc 取消、Backspace 退点；
   量算（M2-CTRL-10）实时显示长度/面积，结果可读可清；
7. 回放：宿主加载带时间戳的轨迹后，可用模块时间轴（`<ReplayBar />`）播放/暂停/倍速/拖动定位/自动跟随；数据与存储由宿主负责（M2-DRAW-17 / 18 / M2-API-16）；
8. 多底图：声明"影像 + 路网"两套本地底图后，可任意互切且**整幅替换**，切换后视角、图元、图层开关、控件开关全部保持，宿主收到通知（M2-MAP-09 / M2-BASE-09 ~ 12）；
9. 按 M2-BASE-05 / M2-API-06 设定地面分辨率上限后，放大到更细层级不再请求新瓦片；该上限不影响在线样式底图（M2-BASE-13）。

**接口验收**

1. 宿主代码中不出现 `maplibre-gl` 的直接调用与模块内部路径；
2. 宿主传入的数据对象缺字段时不报错、按约定降级；
3. 导出/导入图元快照后画面一致；视图状态导出/恢复后画面一致；
4. 第三方按《接口文档》+《接入指南》能在自己的项目中跑起来。

**性能与可靠性验收**

1. 复现基准脚本（`examples/benchmark.ts`），各项耗时不超过 §4 基线；
2. 注入非法图元数据后，其余图元正常渲染（M2-NFR-10）；
3. 断开局域网瓦片后地图仍可打开与交互（M2-BASE-03 / M2-MAP-10）。

**交付验收**

1. `npm run typecheck`、`npm run build` 通过；
2. 模块目录可直接拷贝到新项目运行；
3. 独立子仓可 clone 后 `npm install && npm run dev` 出图。

---

## 4. 性能基线（本机实测，2026-06-11）

**测试环境**：Windows + 无头 Chrome（`--headless=new`，软件渲染 SwiftShader），画布 1582×748，模块独立宿主 `localhost:5180`。
**口径**：`set` = JS 侧把数据翻译为 GeoJSON 并提交给 MapLibre 的墙钟耗时；`回读` = 每帧把画布拷贝到 400×300 并取像素的中位耗时（反映渲染负载）；单机同口径可比。
**复现脚本**：[`examples/benchmark.ts`](../examples/benchmark.ts)（在独立宿主控制台加载后执行 `runBenchmark()`，输出同口径表格）。

| 负载 | 单次提交耗时 | 每帧像素回读 |
|---|---|---|
| 空载（仅底图） | — | 3.1 ms |
| 1,000 无人机点 | 1.2 ms | 2.6 ms |
| 5,000 无人机点 | 3.3 ms | 2.8 ms |
| 20,000 无人机点 | 15.2 ms | 3.1 ms |
| 50,000 无人机点 | 32.4 ms | 3.7 ms |
| 500 区域多边形 | 1.4 ms | 3.2 ms |
| 2,000 区域多边形 | 3.7 ms | 2.6 ms |
| 5,000 区域多边形 | 8.7 ms | 2.3 ms |
| 混合：5,000 点 + 500 面 + 500 线 + 500 标注 | 5.1 ms（合计） | 3.0 ms |
| 单点更新（遥测路径，300 点时） | 约 1.7 ms/次 | — |
| JS 堆占用（混合负载后） | 约 58 MB | — |

**结论与当前验收门槛建议**

1. **单次批量提交**：≤ 20,000 个点位或 ≤ 5,000 个多边形时，单次提交 ≤ 20 ms（一帧内可完成）→ 建议门槛：**20,000 点 / 5,000 面 单次提交 ≤ 20 ms**；
2. **单点更新**：≤ 2 ms/次 → 遥测 1 Hz / 10 Hz 场景余量充足（10 Hz × 300 点 = 约 5 ms/s）；
3. **渲染负载**：在当前口径下从空载到 50,000 点，回读耗时仅从 3.1 ms 增至 3.7 ms，**未观察到拐点**——说明该量级下瓶颈不在 MapLibre 渲染，而在数据翻译与提交（JS 侧）。

**尚未覆盖（需真机补测）**：无头软件渲染下无法反映 GPU 极限，因此**标签（text）数量**与**大面数填充**的真实上限需在目标终端（平板/办公机）上复测；建议补一份真机基线纳入 M2-NFR-11。

---

## 5. 决策记录

| 编号 | 议题 | 决策 | 影响 |
|---|---|---|---|
| D1 | 瓦片精度上限的**表达方式** | **按地面分辨率（米/像素）**表达（接口同时接受等效层级） | M2-BASE-05 / M2-API-06 参数形态 |
| D2 | 精度上限的**默认值** | **默认不限制**：不设上限时视口在哪一级就用哪一级瓦片；设了上限才有"封顶"效果 | 开箱行为 |
| D3 | 精度上限的**边界行为** | 硬边界、无递减：超出上限的分辨率请求**直接不发**，画面用已加载的较粗瓦片放大显示 | 观感：超过上限后画面变"放大"而非"变糊块" |
| D4 | 精度上限是否作用于在线底图 | **不作用**：在线底图随在线源；在线卡顿属在线源自身问题 | M2-BASE-13 |
| D5 | 手绘交互与图元编辑 | **同期做**（一次做完，不分批） | M2-DRAW-08 / 12 |
| D6 | 时间维度回放的实现分工 | **模块提供时间轴**（播放控制 + 按时刻渲染）；**数据与存储由宿主负责**，模块只提供加载/替换/清空接口 | M2-DRAW-17 / 18 / M2-API-16 |
| D7 | 多实例地图 | **纳入待完成**（不设"期"） | M2-NFR-08 |
| D8 | 军事标绘符号库标准 | **国军标（GJB）** | M2-DRAW-16 |
| D9 | 是否接国内在线底图源 | **否**：底图数据下载到本地后不再依赖在线源；坐标系适配需求随之取消 | 取消原 M2-MAP-08（坐标系适配） |
| D10 | 图元容量指标 | **以 §4 实测基线为准**（不再用拍脑袋数字） | M2-NFR-07 / 11 |
| D11 | 是否发布 npm 包 | **暂不发布**，后续再议 | 交付形态 |
| D12 | 底图包分类口径 | 按"**每一套完整底图**"组织：切哪套就**整幅替换**（卫星影像、路网、地形各为一套），不做同类混合 | M2-MAP-09 / M2-BASE-10；取消"影像+标注叠加/透明度"需求 |
| D13 | 多套底图的层级关系 | **单套生效**：同一时刻只有一套底图在显示，切换即整幅替换；不提供叠加层与透明度调节 | 同上 |
| D14 | 服务端是否下发底图清单 | **可选**：模块本身只要一份清单对象；清单从哪来由宿主决定（写在前端配置里，或由后端接口下发） | M2-API-10；后端接口属主仓契约范围 |

---

## 6. 需求追踪

> 与 §0 完成情况总览同源；§0 另有分域覆盖度与逐条清单。

| 域 | 条目总数 | 已完成 | 待完成 |
|---|---|---|---|
| M2-MAP 地图能力 | 10 | 01–08（8） | 09、10（2） |
| M2-CTRL 地图控件 | 15 | 01–15（15） | — |
| M2-BASE 底图 | 13 | 01–13（13） | — |
| M2-LAYER 图层与图元 | 6 | 01–06（6） | — |
| M2-DRAW 绘制接口 | 18 | 01–18（18） | — |
| M2-API 对外接口 | 17 | 01–17（17） | — |
| M2-NFR 非功能性 | 14 | 01–06、10、14（8） | 07–09、11–13（6） |
| **合计** | **93** | **85** | **8** |

> 计数口径：一条需求算一条。"已完成"= 状态为 ✅；"待完成"= 状态含 ⏳（当前无部分完成项）。
> **第一轮**（控件按需显示 / 瓦片精度上限 / 图元显隐 / 批量提交）完成 10 条；
> **第二轮**（多套底图注册·切换·通知 / 图元点击悬停事件 / 错误边界 / 运行指标）完成 11 条；
> **第三轮**（绘制对象补全：航线 / 圆形·椭圆区域 / 目标区域，共 11 类图元；命中检测覆盖全部图元）完成 1 条；
> **第四轮**（地图内图例、图层排序与分组透明度、精度上限作用范围验证）完成 3 条；
> **第五轮**（手绘交互、图元编辑、吸附对齐、量算、键盘操作）完成 5 条；
> **第六轮**（视图状态序列化、图片导出、接口最小示例、接口稳定性约定、第三方接入指南、渲染时机合并）完成 6 条；
> **第七轮**（时间轴回放、回放数据接口、回放控制接口）完成 3 条，M2-API 域 17/17 全部完成；
> **第八轮**（圈层图元、目标聚合、标签避让与分级）完成 3 条；
> **第九轮**（样式模板、主题热切换）完成 2 条，M2-DRAW 域 18/18 全部完成；
> **第十轮**（国军标标绘符号库、显示模式手动切换）完成 2 条，M2-CTRL 与 M2-DRAW 两域全部完成；
> **第十一轮**（军用网格与经纬网、瓦片包版本与完整性校验）完成 2 条，M2-BASE 域 13/13 全部完成。
> ⚠️ 同轮的 **M2-MAP-10 瓦片源自动降级未达成**：降级判定口径有误（见该条备注），留待下一轮重做。
> **第十二轮**（底图加载观感）完成 1 条：根因是主题把底图压暗到 0.42，导致"正常的暗"与"缺口底色"分不清。
> **跨模块依赖**：若选择"由服务端下发本地底图清单"，需后端新增接口（如 `GET /api/v1/map/basemaps`）；模块侧只消费清单对象，不定义该接口。

---

## 7. 需求补充来源

| 编号 | 内容 | 相关条目 |
|---|---|---|
| 需求补充 1 | 地图控件（指北针/经纬度/缩放按钮/比例尺）默认隐藏，调用接口才显示 | M2-CTRL-01 ~ 05、M2-API-05 |
| 需求补充 2 | 绘制对象接口化（无人机、航线、轨迹、各类区域、目标…）+ 显隐控制 | M2-DRAW-01、03 |
| 需求补充 3 | 离线瓦片精度选择（按分辨率设上限） | M2-BASE-05、M2-API-06、D1 ~ D4 |
| 需求补充 4 | 本地多套底图切换 + 对外接口 | M2-MAP-09、M2-BASE-09 ~ 13、M2-API-10 ~ 13、D12 ~ D14 |
| 需求补充 5 | 手绘与编辑同期、回放分工、国军标符号、多实例、性能基线 | M2-DRAW-08/12/16/17/18、M2-NFR-07/08/11、M2-API-16、D5 ~ D11 |
