# map-2d · 模块文档索引

- 版本：v1.0
- 日期：2026-06-11
- 定位：**`map-2d` 二维地图模块自身的需求与设计文档**。本目录只描述这个模块，不涉及主系统其它部分；主系统的整体文档在仓库 `docs/` 下，二者分工见下。
- 模块定位：一个**只做二维地图渲染**、能被单独拷走或单独启动的模块（源码交付，非 npm 包）

---

## 1. 文档清单

| 文档 | 内容 | 读者 |
|---|---|---|
| [需求文档.md](./需求文档.md) | 模块做什么、不做什么、各自现状（✅/⚠️/❌）、验收口径；需求编号 `M2-xx` | 需求确认、验收、后续排期 |
| [设计文档.md](./设计文档.md) | 模块怎么做的：分层与依赖规则、运行形态、渲染管线、绘制 API、状态与数据流、关键设计决策与取舍、已知边界 | 开发、交接、二次开发 |
| [../README.md](../README.md) | 对外门面：快速上手、公开 API 一览、目录结构、交付方式 | 第一次接触的人 |

---

## 2. 与主系统文档的分工

| 内容 | 归属 |
|---|---|
| 模块内部需求条目（`M2-xx`）、模块级验收 | **本目录**（《需求文档》） |
| 模块内部结构、渲染管线、绘制 API、设计取舍 | **本目录**（《设计文档》） |
| 与主系统的接入方式（`@map2d` 别名、唯一适配器 `useMapData.ts`） | 本目录《设计文档》§8 + 主仓 `docs/05-开发/已实现需求文档.md` |
| 主系统业务需求（阶段流程、目标台账、链路、语音、视频、AI） | 主仓 `docs/`（与地图无关，不进本目录） |
| 地图在主系统里的业务侧需求（TR-MAP/TR-GEO/TR-VIEW、逐阶段呈现矩阵） | 主仓 `docs/02-技术需求/地图相关需求专篇.md` |
| 主系统数据/接口契约（REST、WS、数据模型） | 主仓 `docs/05-开发/开发接口契约规格书.md` |

> 口径：本目录的"现状"以仓库当前代码为准（as-built）；本目录是**模块视角的单一入口**，主仓文档里与本模块相关的部分在此收敛。

---

## 3. 一分钟了解这个模块

```
map-2d/                     独立可交付的二维地图模块（只依赖 maplibre-gl / react / zustand）
├─ doc/                     ← 本目录：模块需求 + 设计
├─ src/
│  ├─ index.ts              唯一公开入口（嵌入方只从这里 import）
│  ├─ core/                 契约类型 / 模块开关 / 视角命令 / UI 状态 / 实例单例
│  ├─ primitives/api.ts     绘制 API：MapDraw（9 类图元）
│  ├─ render/               图层与数据源管理（10 个图层分组）
│  ├─ ui/                   MapView（唯一渲染入口）/ 工具条 / 图层面板 / 指北针
│  └─ standalone/           独立宿主：npm run dev 打开即出图，不依赖后端
└─ README.md                快速上手与公开 API
```

两条使用方式：

| 方式 | 命令 / 代码 | 说明 |
|---|---|---|
| 独立运行（演示、交付、调试） | `npm install && npm run dev` → `http://localhost:5180/` | 不依赖任何后端，自带示例图元 |
| 嵌入宿主（主系统即此种） | `import { MapView, MapDraw } from '@map2d'` | 数据经 props 注入，模块不读宿主 store |

---

## 4. 快速状态摘要（详见《需求文档》§3/§4）

| 能力 | 现状 |
|---|---|
| 二维漫游（拖拽平移 / 滚轮·双击·按钮缩放 / 比例尺 / 指北针 / 禁旋转） | ✅ |
| 绘制 API（9 类图元增删改查、导入导出） | ✅ |
| 图层分组独立开关（10 组） | ✅ |
| 清屏模式、显示模式徽标、视角命令 | ✅ |
| 底图来源（本地栅格瓦片 / 在线样式） | ✅ |
| 手绘交互（鼠标画点/线/面：测距、区域绘制、标绘） | ❌ 未实现（工具为占位） |
| 显示模式手动切换 | ⚠️ 仅随场景+阶段自动 |
| 底图加载观感优化（缩放/拖拽时的深蓝方块） | ⏸ 暂缓（已定位成因，见《需求文档》§6） |
