# map-2d · 模块文档索引

- 版本：v1.0
- 日期：2026-06-11
- 定位：**`map-2d` 二维地图模块自身的需求与设计文档**。本目录只描述这个模块，不涉及主系统其它部分；主系统的整体文档在仓库 `docs/` 下，二者分工见下。
- 模块定位：一个**只做二维地图渲染**、能被单独拷走或单独启动的模块（源码交付，非 npm 包）

---

## 1. 文档清单

| 文档 | 内容 | 读者 |
|---|---|---|
| [接口文档.md](./接口文档.md) | **接入的唯一依据**：快速开始、数据契约、组件/绘制/命令/状态全部接口签名、错误与降级约定、接口速查表。**§1–§9 = 已实现接口（直接用）；§10 = 待实现接口的设计（先别用）** | 接入方（主系统界面层、第三方） |
| [需求文档.md](./需求文档.md) | 项目需求文档格式：概述（定位/性能优先原则/运行形态/边界）、`M2-域-序号` 需求条目、验收准则、**实测性能基线**、**决策记录**、需求追踪（完成 36 / 待完成 57） | 需求确认、验收、后续排期 |
| [设计文档.md](./设计文档.md) | **§1–§9 = 已实现设计**（分层与依赖规则、渲染管线、绘制 API、状态与数据流、关键取舍）；**§10 = 待实现能力的设计方案**（控件按需显示、图元显隐、多底图切换、瓦片精度上限、回放、手绘编辑、国军标符号、多实例、大数据量降级） | 开发、交接、二次开发 |
| [验收台使用说明.md](./验收台使用说明.md) | **不读代码就能看完成情况**：
pm run dev:host 打开后右侧面板逐个按钮验什么、怎么自动化自检 | 验收人、需求方 |
| [../README.md](../README.md) | 对外门面：模块是干什么的、空白机器怎么跑通（含 Release 底图放哪）、功能清单、接入与排障 | 第一次接触的人 |

**三份文档的对应关系**：需求（做什么、完成没有）→ 设计（怎么做、为什么这么做）→ 接口（怎么调）。
每份文档都把自己的**已实现**与**待实现**分开标注，不混着写。

---

## 2. 与主系统文档的分工

| 内容 | 归属 |
|---|---|
| 模块自身的功能清单（已有 / 打算做） | **本目录**（《需求文档》） |
| 模块内部结构、渲染管线、绘制 API、设计取舍 | **本目录**（《设计文档》） |
| 与主系统的接入方式（`@map2d` 别名、唯一适配器 `useMapData.ts`） | 本目录《设计文档》§8 + 主仓 `docs/05-开发/已实现需求文档.md` |
| 主系统业务需求（阶段流程、目标台账、链路、语音、视频、AI） | 主仓 `docs/`（与地图无关，不进本目录） |
| 地图在主系统里的业务侧需求（TR-MAP/TR-GEO/TR-VIEW、逐阶段呈现矩阵） | 主仓 `docs/02-技术需求/地图相关需求专篇.md` |
| 主系统数据/接口契约（REST、WS、数据模型） | 主仓 `docs/05-开发/开发接口契约规格书.md` |

> 口径：本目录只描述这个模块本身——**已经有的功能、打算做的功能、以及怎么做**；主系统的整体需求与接口契约仍以主仓 `docs/` 为准。

---

## 3. 一分钟了解这个模块

```
map-2d/                     独立可交付的二维地图模块（只依赖 maplibre-gl / react / zustand）
├─ doc/                     ← 本目录：模块需求 + 设计
├─ src/
│  ├─ index.ts              唯一公开入口（嵌入方只从这里 import）
│  ├─ core/                 契约类型 / 模块开关 / 视角命令 / UI 状态 / 实例单例
│  ├─ primitives/api.ts     绘制 API：MapDraw（9 类图元）
│  ├─ render/               图层与数据源管理（10 个图层分组）
│  ├─ ui/                   MapView（唯一渲染入口）/ 工具条 / 图层面板 / 指北针
│  └─ standalone/           独立宿主：npm run dev 打开即出图，不依赖后端
└─ README.md                快速上手与公开 API
```

两条使用方式：

| 方式 | 命令 / 代码 | 说明 |
|---|---|---|
| 独立运行（演示、交付、调试） | `npm install && npm run dev` → `http://localhost:5180/` | 不依赖任何后端，自带示例图元 |
| 嵌入宿主（主系统即此种） | `import { MapView, MapDraw } from '@map2d'` | 数据经 props 注入，模块不读宿主 store |

---

## 4. 快速状态摘要（详见《需求文档》§4 需求追踪）

**已实现（P1 基线）**

| 域 | 条目 | 实现位置 |
|---|---|---|
| 地图能力 | 拖拽平移、光标为中心缩放、二维锁定、视角命令、视口回写 | `ui/MapView.tsx`、`core/commands.ts` |
| 底图 | 本地栅格瓦片、在线样式、无瓦片兜底、缺口地面色、运行中切换不丢图元 | `ui/MapView.tsx` |
| 图层与图元 | 10 组独立开关、数据驱动渲染、增量更新、脉冲动画 | `render/LayerManager.ts` |
| 绘制接口 | 增删改查、快照导入导出、公里转像素、样式覆盖、底图切换重放 | `primitives/api.ts` |
| 模块 UI | 工具条（选择/图层/清屏/全屏）、图层面板、显示模式徽标 | `ui/`、`core/` |
| 交付 | 独立宿主、类型检查与构建、子仓发布 | `src/standalone/`、`package.json` |

**需求总量（详见《需求文档》§6）**：93 条 —— **已完成 61 条**，待完成 32 条。**不设"期/阶段"划分，只有完成与待完成**；决策记录 14 条见《需求文档》§5，性能基线见 §4。

**待完成里优先级最高的几组**

| 组 | 需求 | 内容 | 来源 |
|---|---|---|---|
| 接口 | M2-API-14 / 15 / 17 | **接口文档与接入指南**：接口文档已成文，待补最小示例、变更记录、第三方接入指南 | 项目 |
| 性能 | M2-NFR-07 / 11 ~ 14 | **性能基线（已实测，见 §4）**、资源上限、大数据量降级、渲染时机控制 | 项目 |
| 控件 | M2-CTRL-01 ~ 05 / M2-API-05 | **控件按需显示**：指北针、鼠标经纬度、缩放按钮、比例尺默认隐藏，调用接口才显示 | 需求补充 1 |
| 底图 | M2-MAP-09 / M2-BASE-09 ~ 13 / M2-API-10 ~ 13 | **多套本地底图整体切换**：枚举 / 切换 / 切换后状态保持 / 通知；按地面分辨率设精度上限（默认不限制、只作用于本地底图） | 需求补充 3、4 |
| 绘制 | M2-DRAW-01 / 03 / 08 / 12 | 绘制对象补全（航线、圆形/椭圆区域、目标区域）、图元显隐、**手绘与编辑同期做** | 需求补充 2、5 |
| 回放 | M2-DRAW-17 / 18 / M2-API-16 | **模块提供时间轴**（播放/暂停/倍速/定位），数据与存储由宿主负责，模块只留加载/替换/清空接口 | 需求补充 5 |
| 态势 | M2-DRAW-09 ~ 11、16 / M2-CTRL-12 | 圈层图元、目标聚合、标签避让、图层排序、**国军标符号库** | 评审、需求补充 5 |
| 工程 | M2-NFR-08 / 10 / M2-CTRL-15 | 多实例隔离、错误边界、性能观测接口 | 评审、需求补充 5 |
