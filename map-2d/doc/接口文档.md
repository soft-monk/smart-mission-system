# map-2d 二维地图模块 · 接口文档

| 项 | 内容 |
|---|---|
| 模块标识 | `@mapapp/map-2d`（源码模块） |
| 文档版本 | v2.0 |
| 日期 | 2026-06-11 |
| 本文定位 | **接入的唯一依据**：只看本文即可把地图用起来，不需要读源码 |
| 适用对象 | 需要在地图里画态势的模块（主系统界面层、第三方项目） |
| 依据 | 《需求文档》v3.1（93 条需求：**已完成 87 / 待完成 6**） |
| 约定 | 所有导出均来自模块公开入口；`MapData` 等类型结构兼容即可直接传（TS 结构化类型），不要求引用模块类型 |
| 口径 | **§1–§9 与 §10 中"已实现补充"的小节 = 可直接用**；**§10 其余小节 = 待实现接口的设计**（拟定签名，代码未落地）；§11 起为接入、错误、速查 |

---

## 0. 怎么读本文

| 你要 | 看 |
|---|---|
| 马上接入、能用起来的接口 | §1–§9（**已实现**：组件、数据契约、MapDraw、mapCommands、LayerManager、UI 状态、开关、接入） |
| 控件 / 图元显隐 / 批量 / 精度上限 / 底图管理 / 图元事件 / 运行指标 | §10.1 ~ §10.4、§10.6、§10.8（**这些已经实现，可直接用**） |
| 回放（时间轴/播放控制/数据接口） | §10.5（**已实现**） |
| 出问题时的约定 | §12 错误与降级 |
| 一页速查 | §13 接口速查表 |

---

## 1. 快速开始

```ts
// 1) 安装依赖（源码模块：把 map-2d/ 目录放进项目，或指向本地路径）
//    仅需 maplibre-gl / react / zustand

// 2) 在入口注册样式
import 'maplibre-gl/dist/maplibre-gl.css'

// 3) 挂地图
import { MapView, MapToolbar, Compass, MapDraw } from '@map2d'

export const Situation: React.FC = () => (
  <div style={{ position: 'absolute', inset: 0 }}>
    <MapView data={mapData}>{/* children 作为地图内浮层 */}
      <MapToolbar />
      <Compass />
    </MapView>
  </div>
)

// 4) 画图（不碰 MapLibre）
MapDraw.set('target', [{ id: 'T-1', lng: 116.45, lat: 39.88, threat: 'high' }])
```

`mapData` 的形状见 §3.1；最小可用对象只要求 `config` 与各数组字段。

---

## 2. 导出清单（公开入口的全部能力）

| 分组 | 导出 |
|---|---|
| 地图容器与控件 | `MapView`、`MapToolbar`、`MapModeBadge`、`LayerPanel`、`Compass` |
| 绘制接口 | `MapDraw` |
| 视角命令 | `mapCommands` |
| 底层图层管理 | `LayerManager`、`LAYER_GROUP_LABELS`、`ALL_LAYER_GROUPS` |
| 模块 UI 状态 | `useMapUiStore`、`LAYER_GROUPS` |
| 显示模式 | `DISPLAY_MODE`、`displayModeOf` |
| 模块开关 | `MAP_OPTIONS` |
| 地图实例（高级） | `mapInstance` |
| 类型 | `MapData`、`MapConfigData`、`MapViewport`、`MapToolKey`、`Group`、`LinkEdge`、`LinkTopology`、`Target`、`TargetTrackPoint`、`UavPosEvent`、`Phase`、`ScenarioKey`、`LayerGroup`、`MapOptions`、`PrimitiveKind`、`AreaItem`、`DroneItem`、`TargetItem`、`LinkItem`、`TrackItem`、`ScanItem`、`PulseItem`、`ClusterItem`、`LabelItem`、`DrawSnapshot` |

---

## 3. 数据契约

### 3.1 `MapData`（地图容器的唯一入参）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `config` | `MapConfigData \| null` | 是（可为 null） | 视角与底图配置；为 null 时用模块内置默认视角 |
| `scenarioKey` | `string` | 是 | 场景标识（影响分区与阶段图层） |
| `phase` | `'T0'…'T7'` | 是 | 当前阶段（影响哪些图层可见） |
| `targets` | `Target[]` | 是 | 目标台账 |
| `selectedTargetId` | `string \| null` | 否 | 当前选中目标（触发锁定框） |
| `groups` | `Group[]` | 是 | 集群编组 |
| `uavs` | `UavPosEvent[]` | 是 | 无人机实时位置 |
| `edges` | `LinkEdge[]` | 是 | 链路边 |
| `topology` | `LinkTopology \| null` | 是 | 链路拓扑（提供节点坐标） |
| `track` | `TargetTrackPoint[]` | 是 | 目标轨迹点 |

> 字段缺失时按"空数组/默认值"处理，不抛错（便于宿主逐步接入）。

### 3.2 `MapConfigData`

| 字段 | 类型 | 说明 |
|---|---|---|
| `center` | `[lng, lat]` | 初始中心 |
| `zoom` | `number` | 初始层级 |
| `minZoom` / `maxZoom` | `number` | 缩放范围 |
| `basemap.tileUrlTemplate` | `string` | 本地瓦片模板，如 `/tiles/raster/{z}/{x}/{y}.jpg` |
| `basemap.styleUrl` | `string?` | 在线样式 URL；**优先级高于瓦片模板** |
| `basemap.attribution` | `string` | 版权署名文本（是否显示由 `MAP_OPTIONS.showAttribution` 决定） |
| `basemap.fallback` | `string?` | 契约兼容字段（当前前端不据此改色） |
| `video` | `{name,url}[]?` | 与地图无关的透传字段，便于宿主复用同一配置对象 |

### 3.3 图元类型（`MapDraw` 的入参形状）

| 类型 | 关键字段 | 必填 |
|---|---|---|
| `AreaItem`（区域） | `id`、`polygon: [lng,lat][]`、`color?`、`label?`、`dashed?`、`opacity?` | id、polygon |
| `DroneItem`（无人机） | `id`、`lng`、`lat`、`type?`(optical/radar/electronic/comm)、`color?`、`label?` | id、lng、lat |
| `TargetItem`（目标） | `id`、`lng`、`lat`、`threat?`(high/mid/low)、`status?`(red/yellow/gray)、`color?`、`label?`、`selected?` | id、lng、lat |
| `LinkItem`（链路） | `id`、`from: [lng,lat]`、`to: [lng,lat]`、`state?`(green/yellow/red)、`color?`、`label?` | id、from、to |
| `TrackItem`（轨迹） | `id`、`points: [lng,lat][]`、`color?`、`dashed?` | id、points |
| `ScanItem`（扫描覆盖） | `id`、`lng`、`lat`、`radiusKm`、`color?`、`label?` | id、lng、lat、radiusKm |
| `PulseItem`（脉冲） | `id`、`lng`、`lat`、`color?`、`radiusKm?` | id、lng、lat |
| `ClusterItem`（集群） | `id`、`lng`、`lat`、`name?`、`color?` | id、lng、lat |
| `LabelItem`（标注） | `id`、`lng`、`lat`、`text`、`color?`、`size?`、`radius?` | id、lng、lat、text |

**默认配色**（未给 `color` 时）：

| 场景 | 取值 |
|---|---|
| 威胁 | high `#ef4444` ｜ mid `#f59e0b` ｜ low `#22d3ee` |
| 目标状态 | red `#ef4444` ｜ yellow `#f59e0b` ｜ gray `#8b93a7` |
| 无人机类型 | optical `#22d3ee` ｜ radar `#f59e0b` ｜ electronic `#a855f7` ｜ comm `#22c55e` |
| 链路状态 | green `#22c55e` ｜ yellow `#f59e0b` ｜ red `#ef4444` |
| 其它 | 区域 `#22d3ee` ｜ 轨迹 `#ef4444` ｜ 扫描 `#38bdf8` ｜ 脉冲 `#22d3ee` ｜ 集群 `#8b5cf6` ｜ 标注 `#cfe3f5` |

---

## 4. 组件接口

### 4.1 `<MapView data={MapData}>`

| prop | 类型 | 必填 | 说明 |
|---|---|---|---|
| `data` | `MapData` | 是 | 见 §3.1。**配置只在挂载时读取一次**；运行中改视角请用 `mapCommands` 或改 `config` 对象（会触发平滑移动） |
| `children` | `ReactNode` | 否 | 渲染在地图之上的浮层（工具条、图层面板、指北针等） |

行为约定：

- 拖拽平移、滚轮/双击缩放、二维锁定（禁旋转与俯仰）、比例尺与缩放按钮（当前默认显示，后续改为按需，见需求 M2-CTRL-01）；
- 换 `config.center/zoom` → 平滑移动（600 ms）；换 `basemap` → 重建样式并**自动重放已绘图元**；
- 瓦片失败只累计告警，不隐藏图层；
- 卸载时销毁地图实例。

### 4.2 `<MapToolbar />`

无 props。提供：选择 / 图层（展开图层面板）/ 清屏 / 全屏。测距、区域、新建、标绘为占位（禁用状态）。

### 4.3 `<LayerPanel />`

无 props。10 个图层分组的开关面板，开关状态跨底图切换保留。也可由工具条的「图层」按钮控制显隐。

### 4.4 `<Compass />`

无 props。右下区域常显指北针，随 bearing 旋转，点击复位正北（当前恒指正北，因地图锁定二维）。

### 4.5 `<MapModeBadge mode={string} />`

| prop | 类型 | 说明 |
|---|---|---|
| `mode` | `string` | 显示模式名称；通常用 `displayModeOf(scenarioKey, phase)` 取 |

---

## 5. 绘制接口 `MapDraw`

所有方法同步返回；数据在模块内部按类型集合管理，变更即触发对应图层增量刷新。

| 方法 | 签名 | 说明 |
|---|---|---|
| `set` | `set<K>(kind: K, items: DrawSnapshot[K])` | **整组替换**该类型全部图元 |
| `add` | `add<K>(kind: K, item: DrawSnapshot[K][number])` | 新增或更新单个图元（按 `id` 覆盖） |
| `remove` | `remove(kind, id: string)` | 删除单个图元 |
| `clear` | `clear(kind?)` | 不传清空全部图元；传则清空该类 |
| `list` | `list<K>(kind: K): DrawSnapshot[K]` | 读取该类图元副本 |
| `export` | `export(): DrawSnapshot` | 导出全部图元（可 JSON 序列化） |
| `load` | `load(snapshot: Partial<DrawSnapshot>)` | 导入快照（整体覆盖） |
| `render` | `render()` | 手动重绘（换底图后由模块自动调用；宿主一般不需要） |

`kind` 取值（`PrimitiveKind`）：`'area' | 'drone' | 'target' | 'link' | 'track' | 'scan' | 'pulse' | 'cluster' | 'label'`

```ts
// 示例：区域 + 目标 + 扫描覆盖
MapDraw.set('area', [{ id: 'A-1', polygon: [[116.30,39.94],[116.42,39.95],[116.44,39.88]], color: '#3b82f6', label: 'A 区' }])
MapDraw.add('target', { id: 'T-1', lng: 116.452, lat: 39.878, threat: 'high', status: 'red', selected: true })
MapDraw.add('scan', { id: 'S-1', lng: 116.365, lat: 39.912, radiusKm: 6, label: '光电侦察覆盖' })

// 存档 → 恢复
const snap = MapDraw.export()
MapDraw.clear()
MapDraw.load(snap)
```

**注意事项**

- `radiusKm` 按当前缩放换算成像素；缩放变化时模块会自动重算（模块内部监听 `zoomend`）；
- 扫描/脉冲的半径是**地理尺寸**，不会因缩放而改变实际覆盖范围；
- 同一 `id` 重复 `add` 视为更新；`remove` 时若 `id` 不存在则无副作用。

---

## 6. 视角命令 `mapCommands`

| 方法 | 签名 | 说明 |
|---|---|---|
| `setView` | `setView(lng, lat, zoom?, durationMs = 0)` | 设置视角；`durationMs > 0` 时平滑过渡，否则瞬间定位（`zoom` 省略保持当前） |
| `flyTo` | `flyTo(lng, lat, zoom?, durationMs = 900)` | 飞行（带抛物线过渡） |
| `focus` | `focus(lng, lat, zoom = 13)` | 聚焦某点，默认街道级 |
| `resetView` | `resetView(config?: MapConfigData \| null)` | 复位到配置的初始视角（无配置时回北京 z11） |
| `zoomBy` | `zoomBy(delta)` | 相对缩放（正数放大） |
| `getViewport` | `getViewport(): MapViewport` | 读取当前视口 `{lng, lat, zoom, bearing}`；未就绪返回全 0 |
| `isReady` | `isReady(): boolean` | 地图是否已创建，可用于轮询等待 |

```ts
if (mapCommands.isReady()) mapCommands.focus(116.452, 39.878, 14)
mapCommands.flyTo(116.4, 39.9, 12)
const vp = mapCommands.getViewport()
```

---

## 7. 图层管理 `LayerManager`（高级用法）

一般不需要直接调用（`MapDraw` 已覆盖绘制需求）；以下两类场景会用到：**批量灌业务数据**、**直接写 GeoJSON**。

**分组显隐**

| 方法 | 说明 |
|---|---|
| `setGroupVisible(group: LayerGroup, visible: boolean)` | 开关某分组（10 组：area/pulse/scan/link/group/track/trail/target/uav/mark） |
| `isGroupVisible(group)` / `hiddenGroups()` | 查询 |
| `applyVisibility()` | 把当前显隐状态重新应用（幂等） |

**业务数据入口（给数组即出图）**

| 方法 | 说明 |
|---|---|
| `setTargets(targets, selectedId?)` | 目标（含选中态） |
| `setGroups(groups)` | 集群编组 |
| `setUavs(list)` | 无人机位置 |
| `setLinks(edges, nodes)` | 链路（节点坐标来自 `nodes`） |
| `setTrack(points)` | 目标轨迹 |
| `setScenario(key)` / `setPhase(phase)` | 场景与阶段（影响分区与图层可见性） |

**坐标/GeoJSON 显式入口**

`setAreaFeatures(fc)`、`setScanFeatures(fc)`、`setLinkFeatures(fc)`、`setGroupFeatures(fc)`、`setTargetFeatures(fc)`、`setUavFeatures(fc)`、`setTrackFeatures(fc)`、`setMarkers(fc)`、`setPulseItems(seeds)`、`setPulseSeedsPublic(seeds)`、`clearAll()`

> `clearAll()` 只清动态图层，不影响底图。

---

## 8. 模块 UI 状态 `useMapUiStore`

```ts
const clearMode = useMapUiStore((s) => s.clearMode)
const toggleClearMode = useMapUiStore((s) => s.toggleClearMode)
```

| 字段 / 方法 | 类型 | 说明 |
|---|---|---|
| `clearMode` / `toggleClearMode()` / `setClearMode(v)` | `boolean` | 清屏开关量。**模块只置位**；隐藏哪些宿主面板由宿主自己执行 |
| `activeTool` / `setActiveTool(t)` | `MapToolKey` | 当前工具 |
| `layersOpen` / `toggleLayersPanel()` / `setLayersPanel(open)` | `boolean` | 图层面板 |
| `hiddenGroups` / `toggleGroup(g)` | `LayerGroup[]` | 被关闭的图层分组 |
| `viewport` / `setViewport(v)` | `MapViewport` | 视口（地图事件自动回写） |
| `displayMode` / `setDisplayMode(m)` | `string` | 显示模式名称，由宿主写入 |

`LAYER_GROUPS` 为分组清单（顺序固定），配合 `LAYER_GROUP_LABELS` 渲染中文名。

---

## 9. 辅助函数与开关

| 导出 | 签名 / 值 | 说明 |
|---|---|---|
| `displayModeOf` | `(scenarioKey, phase) => string` | 按场景 + 阶段取显示模式名（10 种取值，见 `DISPLAY_MODE`） |
| `MAP_OPTIONS.showAttribution` | `boolean`，默认 `false` | 是否显示版权署名；关闭时**不写入样式署名**（合规责任由使用方承担） |
| `MAP_OPTIONS.compactAttribution` | `boolean`，默认 `true` | 署名紧凑模式 |
| `MAP_OPTIONS.rasterFadeDuration` | `number`，默认 `0` | 瓦片淡入时长；0 = 到达即显示 |
| `mapInstance` | `{ current: MapLibreMap \| null }` | 高级逃生口：需要直接调用 MapLibre 时使用（**不建议**，会破坏模块封装） |

---

## 10. 新能力接口（本批已实现 + 待实现的设计）

> **已实现（可直接用）**：§10.1 控件按需显示、§10.2 图元显隐与批量、§10.3 底图管理、§10.4 瓦片精度上限、
> §10.6 图元事件、§10.8 运行指标与错误上报。
> 本节所列能力**均已实现**。
> §1–§9 是模块原有接口；与本节冲突时**以实际导出为准**（看 `src/index.ts` 或跑
> `node -e "import('@map2d').then(m=>console.log(Object.keys(m)))"`）。

### 10.1 控件按需显示（M2-API-05）— ✅ 已实现

```ts
import { mapCommands, MAP_OPTIONS } from '@map2d'

mapCommands.showControls(['compass', 'scale'])          // 只显示这两个
mapCommands.showControls(['coords'], false)             // 关闭某一类
mapCommands.toggleControl('zoom')                        // 单个切换
mapCommands.getControls()                                // → ['compass', 'scale']
mapCommands.getControlState()                            // → { compass:true, coords:false, zoom:false, scale:false }

// 或改默认值（模块初始化时读一次；适合"某类控件永远显示"的项目）
MAP_OPTIONS.controls = { compass: true, coords: true, zoom: false, scale: false }
```

**自绘控件的渲染位置**：`<Compass />` 与 `<CoordReadout />` 读同一份状态，宿主把它们放进 `<MapView>` 即可；
不想用模块状态时，给 `<Compass force />` 可强制显示（由宿主自己决定何时渲染）。

| 控件 key | 说明 | 默认 |
|---|---|---|
| `compass` | 指北针（随方向旋转、点击复位正北） | 不显示 |
| `coords` | 鼠标位置经纬度（跟随光标刷新） | 不显示 |
| `zoom` | 缩放按钮 `+ / −` | 不显示 |
| `scale` | 比例尺（公制） | 不显示 |

**约定**：不调用 `showControls` 时，地图上只有底图与绘制物——对应 M2-CTRL-01 的验收（"未调用接口时地图上只有底图与绘制物"）。

### 10.2 图元显隐与批量提交（M2-DRAW-03 / M2-API-07）— ✅ 已实现

```ts
// 单个图元显隐：数据保留，只是不画
MapDraw.hide('target', 'T-1')
MapDraw.show('target', 'T-1')
MapDraw.setVisible('target', 'T-1', false)
MapDraw.hideAll('drone')       // 整类
MapDraw.showAll()              // 全部恢复

// 批量提交：中间不渲染，退出时按受影响类型各提交一次（单帧渲染）
MapDraw.batch(() => {
  for (let i = 0; i < 2000; i++) MapDraw.add('drone', { id: 'D' + i, lng: 116 + i * 1e-4, lat: 39.9 })
})
// 返回受影响类型清单，例如 { kinds: ['drone'], rendered: 1 }
```

**约定**：`hide` 不影响 `list()` / `export()` 的结果（数据仍在）；`batch` 内可以做任意次增删改，只渲染一次。

### 10.3 底图管理（M2-API-10 ~ 13）— ✅ 已实现

```ts
import { basemaps } from '@map2d'

// 声明清单（初始化时或运行中）
basemaps.register({ id: 'satellite', name: '卫星影像', type: 'raster',
                    tiles: '/tiles/raster/{z}/{x}/{y}.jpg', minZoom: 3, maxZoom: 14,
                    attribution: 'Esri, Maxar' })
basemaps.register({ id: 'road', name: '路网', type: 'raster', tiles: '/tiles/road/{z}/{x}/{y}.png' })
basemaps.register({ id: 'dark', name: '在线深色', type: 'style',
                    styleUrl: 'https://.../style.json' })
basemaps.setList(defs)            // 也可以用一份清单整体设置（清单可从后端接口拉）

// 查询与切换
basemaps.list()                   // → [{ id, name, type, minZoom, maxZoom, metersPerPixel, isCurrent }]
basemaps.current()                // → 当前底图 def
const r = await basemaps.switch('road')
// 成功 → { ok: true, current: {...} }；失败 → { ok: false, reason: 'not-found' | 'already-current' | ... }

// 切换通知
const off = basemaps.onChange((def) => console.log('底图已切到', def.name))
off()                             // 取消订阅

// 运行中增删
basemaps.unregister('road')       // 若移除的是当前底图 → 回落到清单第一套并广播一次变更
```

**约定**（对应 M2-BASE-10 ~ 13 的验收）：

| 项 | 行为 |
|---|---|
| 切换粒度 | **整幅替换**：同一时刻只有一套底图生效，不做叠加与混合 |
| 切换后 | 视角不变、已绘图元不丢、图层分组开关与控件开关保留 |
| 无效 id | 返回 `{ ok: false, reason }`，**不抛异常**、不改动当前底图 |
| 在线底图 | `type: 'style'` 由 `styleUrl` 提供；在线卡顿属在线源自身问题，模块不做特殊处理 |

### 10.4 瓦片精度上限（M2-API-06）— ✅ 已实现

```ts
import { mapCommands } from '@map2d'

// 三种等价写法（按地面分辨率 = 米/像素；也可直接给层级）
mapCommands.setTilePrecisionLimit({ maxMetersPerPixel: 10 })   // 约等于 z14
mapCommands.setTilePrecisionLimit({ maxZoom: 12 })
mapCommands.setTilePrecisionLimit(null)                        // 取消限制（默认）

mapCommands.getTilePrecisionLimit()   // → { maxMetersPerPixel: 10, maxZoom: 14 } | null
```

| 项 | 说明 |
|---|---|
| 默认 | **不限制**：视口在哪一级就用哪一级瓦片 |
| 超限行为 | 硬边界：超过上限的层级**不再请求瓦片**，改用已加载的较粗瓦片放大显示（画面连续但不更清晰） |
| 作用范围 | **仅本地栅格底图**；`type: 'style'` 的在线底图不受约束 |
| 换算关系 | `米/像素(z, lat) ≈ 156543.03392 × cos(lat) / 2^z`；接口以赤道值换算，偏保守 |

### 10.5 回放（M2-API-16）— ✅ 已实现

**组件**：`<ReplayBar />` 放进 `<MapView>`（未加载回放数据时不渲染）。

```tsx
import { MapView, ReplayBar, mapCommands } from '@map2d'

<MapView data={data}>
  <ReplayBar />          {/* 播放/暂停/上一下一采样点/拖拽定位/倍速/自动跟随 */}
</MapView>
```

**数据由宿主提供**（模块不负责存储与持久化）：

```ts
mapCommands.loadReplay({
  id: 'mission-0713',
  tracks: [
    {
      id: 'UAV-01', label: '无人机 01', kind: 'drone',   // kind: 'drone' | 'target' | 'label'
      color: '#22d3ee',
      samples: [
        // t 为毫秒时间戳；heading/speed 可省（模块按相邻点自动推算）
        { t: 1789178400000, lng: 116.44, lat: 39.90 },
        { t: 1789178401000, lng: 116.43, lat: 39.91 },
      ],
    },
  ],
})
```

规则：
- 采样点**按时间自动排序**，无需宿主预排；
- 每条轨迹可设**各自的生效时间范围**：不在范围内的对象**不显示**（例如目标 15 秒后才出现）；
- 点之间按时间**线性插值**，宿主只需给离散点，不必关心帧率；
- `kind: 'drone'` 的对象会额外画出**已走过的尾迹**（模块自带的时间轴可视化）。

**播放控制（M2-API-16，共 12 个接口）**

```ts
mapCommands.playReplay()                       // 播到末尾后再点会从头开始
mapCommands.pauseReplay()
mapCommands.toggleReplay()
mapCommands.setReplaySpeed(4)                  // 0.25–16，自动夹取
mapCommands.seekReplay(1789178420000)          // 定位到毫秒时间戳
mapCommands.seekReplayProgress(0.5)            // 按进度 0–1（时间轴滑块的用法）
mapCommands.stepReplay(1)                      // 步进到下一个采样点（-1 上一个）
mapCommands.setReplayFollow(true)              // 自动跟随：播放时视角跟住第一个活跃对象
mapCommands.getReplayStatus()                  // → { loaded, playing, speed, current, start, end, progress, duration, follow, states, dataId }
mapCommands.onReplayChange(fn)                 // 订阅状态变化（播放中逐帧）；返回取消订阅函数
mapCommands.clearReplay()                      // 清空数据并停止播放，地图恢复"无回放"
mapCommands.loadReplay(data)                   // 加载/替换
```

**状态对象**（`getReplayStatus().states`）只含**该时刻活跃**的对象；要查某个对象在任意时刻的状态
（含未激活）用导出的 `sampleAt(track, t)`。

**自动跟随的实现细节**：用 `jumpTo` 而不是 `easeTo`——回放是逐帧连续的，`easeTo` 每帧都会打断
上一段动画，反而抖动；位置没变化时不移动相机。


**分工**：模块提供时间轴与播放控制；**数据的采集、存储、提供由宿主负责**。

```ts
import { replay } from '@map2d'

// 宿主把带时间戳的轨迹交进来（t 建议为毫秒时间戳，升序）
replay.load({
  tracks: [
    { id: 'U-1', kind: 'drone',   points: [{ t: 0, lng: 116.40, lat: 39.90, alt: 120, heading: 45 }, /* ... */] },
    { id: 'T-1', kind: 'target',  points: [{ t: 0, lng: 116.45, lat: 39.88 }, /* ... */] },
  ],
  range: { from: 0, to: 600000 },   // 可选；不给则取数据的起止时间
})

// 播放控制
replay.play()
replay.pause()
replay.setSpeed(4)          // 倍速：0.5 / 1 / 2 / 4 / 8
replay.seek(120000)         // 定位到某一时刻（毫秒）
replay.step(1000)           // 前进/后退一帧（毫秒，可负）

// 状态查询与订阅
replay.state()              // → { t, playing, speed, from, to, finished }
const off = replay.onTick((s) => chart.update(s.t))   // 宿主联动自己的图表
off()

// 结束
replay.stop()               // 停止播放，保留当前画面
replay.clear()              // 停止并清空回放产生的图元
```

**约定**：模块**不负责持久化与采集**；`load` 之后即可 `play`；`clear()` 之后地图恢复为回放前的图元状态。

### 10.6 图元事件（M2-DRAW-13）— ✅ 已实现（点击/悬停）

**军用网格与坐标换算（M2-MAP-08）**

```ts
mapCommands.setGrid('graticule')      // 经纬网；'utm' UTM 方格；'none' 关闭
mapCommands.getGrid()

// 坐标读数（经纬度 + UTM + MGRS），不依赖地图实例
import { coordinateReadout, toUTM, toMGRS, utmZone } from '@map2d'
coordinateReadout(116.3974, 39.9093)
// → { lng, lat, utm: { zone:50, hemisphere:'N', easting:448494, northing:4417864 },
//     mgrs: '50S DK 48494 17864', zoneBand: '50S' }
toMGRS(116.3974, 39.9093, 3)          // 3 位精度 → '50S DK 484 178'
mapCommands.readCenterCoordinate()    // 地图中心的读数
```

网格间隔随缩放自动选择（经纬网 z≥13 用 0.005°、z≥9 用 0.05°…；UTM 用 1/5/10km），
且会压在业务图层之下。样式重建（换底图、精度上限变更）后网格自动重建。

**瓦片包校验（M2-BASE-08）**

```bash
# 从瓦片包生成清单（版本/各层数量/键列表/sha256）
node scripts/gen-tile-manifest.mjs ./tiles/raster v1
```

```ts
const report = await mapCommands.validateTiles({ samplePerZoom: 3, expectVersion: 'v1' })
// → { status: 'overall' | 'failed' | 'sampled', version, checks[], missing[], probed, elapsedMs }
mapCommands.validateTiles({ manifestUrl: '/tiles/manifest.json' })
```

`checks` 含五项：清单可读、版本匹配、模板一致、层级连续、总数一致，外加抽样可读。
说明：浏览器**无法遍历服务端目录**，所以"缺块清单"来自**清单的数量对比 + 抽样 HEAD 探测**；
要全量核对请比对清单里的 `keys`。

**瓦片源降级（M2-MAP-10）—— 判定口径待重做，接口先可用**

```ts
mapCommands.getTileState()      // → { degraded, reason, errors, loaded, at, samples }
mapCommands.onTilesDegraded(s => console.warn('底图源不可用', s.reason))
mapCommands.getStats().degraded // 运行指标里的降级位
```

⚠️ 当前**降级判定不可靠**：依赖 MapLibre 的 `sourcedata` 事件统计成功数，而该事件（含带
`tile` 字段的）是逐瓦片进度事件，真实页面每秒触发上千次，使"成功数"始终远大于阈值，
降级条件不成立。查询/事件/兜底能力可用，但"自动判定"需要在网络层挂钩后重做。


**国军标标绘符号（M2-DRAW-16）**

```ts
// 一个符号 = 位置 + 符号名 + 敌我属性（框形与颜色随之变化）+ 旋转/缩放
MapDraw.set('symbol', [
  { id: 'SY-1', lng: 116.35, lat: 39.92, symbol: 'command', affiliation: 'friend', label: '指挥所' },
  { id: 'SY-2', lng: 116.33, lat: 39.90, symbol: 'armor',   affiliation: 'friend', rotation: 30 },
  { id: 'SY-3', lng: 116.47, lat: 39.94, symbol: 'radar',   affiliation: 'hostile', label: '雷达站（敌）' },
  { id: 'SY-4', lng: 116.50, lat: 39.87, symbol: 'unknown', affiliation: 'unknown', size: 0.9 },
])

mapCommands.listSymbols()                       // → 内置 12 个 + 宿主注册的
mapCommands.getSymbolSvg('armor', 'hostile')    // → SVG 字符串（可用于图例/面板）
mapCommands.registerSymbol({ key: 'mymortar', name: '迫击炮', svg: (c) => '...' })
```

内置符号 key：`infantry` 步兵、`armor` 装甲、`artillery` 炮兵、`missile` 导弹、`radar` 雷达、
`command` 指挥所、`recon` 侦察、`ew` 电子对抗、`logistics` 后勤、`medical` 卫生、
`aviation` 航空兵、`uav` 无人机。

敌我属性与配色（`AFFILIATION_COLOR`）：`friend` 矩形+蓝、`hostile` 菱形+红、
`neutral` 方块+绿、`unknown` 圆角+黄。

> 实现说明：符号图像由模块**同步逐像素栅格化**（32×32 RGBA，自写线/圆/多边形绘制）。
> 不用 SVG 是因为把 SVG 画进 canvas 需要异步解码，而 `map.addImage` 需要同步拿到像素——
> 同步 `drawImage` 会得到一张空图（实测非空像素为 0，符号完全不可见）。
> 宿主注册的**自定义符号**目前用"通用框 + 中心点"占位（无法逐像素栅格化任意 SVG），
> 需要精细图形请用内置符号或扩展 `symbolRaster.ts` 的绘制表。

**显示模式（M2-CTRL-09）**

```ts
mapCommands.setDisplayMode('任务规划')     // 手动指定，优先于阶段推导
mapCommands.getDisplayMode()
// → { mode: '任务规划', manual: true, derived: '实时态势', scenarioKey, phase }
mapCommands.clearDisplayMode()             // 解除手动，回落推导值
mapCommands.listDisplayModes()             // 可选模式清单（供宿主做下拉）
```

优先级约定（写死在实现里）：
1. **手动值优先**——阶段变化时只更新 `derived`，不动 `mode`；
2. `clearDisplayMode()` 立即回落推导值；
3. **切换场景自动解除手动覆盖**——换任务不该沿用上个任务的显示模式。


**圈层类图元（M2-DRAW-09）**——距离环 / 方位线 / 方位圈 / 九宫格

```ts
MapDraw.set('annulus', [
  // 距离环：一个中心 + 多个半径（公里），画出等距同心圈
  { id: 'R-1', kind: 'ring', lng: 116.40, lat: 39.90, radiusKmList: [5, 10, 15] },
  // 方位线：中心 + 方位角（正北 0、顺时针）+ 长度（公里）
  { id: 'B-1', kind: 'bearing-line', lng: 116.40, lat: 39.90, bearing: 45, lengthKm: 18, dashed: true },
  // 方位圈：整圆 + 每 step 度的刻度（默认 30°）
  { id: 'BR-1', kind: 'bearing-ring', lng: 116.47, lat: 39.90, radiusKm: 8, bearing: 30 },
  // 九宫格参考线：中心 + 边长（公里）+ 行列数（默认 3×3）
  { id: 'G-1', kind: 'grid', lng: 116.33, lat: 39.94, radiusKm: 2, rows: 3, cols: 3, dashed: true },
])
```

几何函数也可单独用（不依赖地图）：

```ts
import { circleRing, bearingRay, gridLines, bearingRingTicks, annulusToLines } from '@map2d'
circleRing([116.4, 39.9], 5)                  // → 96 段闭合环（LngLat[]）
bearingRay([116.4, 39.9], 90, 10)             // → 正东 10km 的射线
gridLines([116.4, 39.9], 2, 3, 3)             // → 8 条参考线
```

**目标聚合（M2-DRAW-10）**

```ts
mapCommands.setClusterOptions({ enabled: true, radiusPx: 42, maxZoom: 13 })
mapCommands.getClusterOptions()
mapCommands.getClusterStats()   // → { input: 60, output: 1, active: true }
```

口径：**按屏幕像素聚类**（不是地理距离）——用户看到的是屏幕，低缩放下 5km 的点就是挤成一团，
高缩放下同样 5km 已经分开；像素口径天然随缩放变化，不用维护"每级阈值表"。
聚类用网格法 O(n)，并在退出聚合条件（未启用 / zoom 超过 `maxZoom`）时**清零气泡与统计**。

**标签避让与分级（M2-DRAW-11）**

```ts
// 逐条分级：图元自带 minZoom，低于该层级不显示（不需要先启用策略）
MapDraw.add('label', { id: 'L-1', lng: 116.4, lat: 39.9, text: '要点', minZoom: 12 })

// 全图数量上限（可选策略）
mapCommands.setLabelPolicy({ enabled: true, maxLabels: 20 })
mapCommands.getLabelPolicy()
```

避让由渲染器内建碰撞检测负责（两层标签层都设了 `text-allow-overlap:false` + `text-ignore-placement:false`），
比手算矩形相交更准；模块只提供"按缩放决定该不该出现"的分级策略。


```ts
const off = MapDraw.on('click', (e) => {
  // e = { kind: 'target', id: 'T-1', lngLat: [116.45, 39.88] }
  openDetailPanel(e.id)
})
MapDraw.on('hover', (e) => { /* 悬停进入/离开 */ })
MapDraw.on('select', (e) => { /* 选中变化 */ })
MapDraw.on('dragend', (e) => { /* 编辑拖拽结束（§10.7 图元编辑） */ })
off()
```

**约定**：点击空白处不触发（回调只带图元）；同一图元多次订阅按注册顺序调用。

### 10.7 手绘与编辑（M2-DRAW-08 / 12 / 14）— ✅ 已实现

**组件**：`<DrawLayer />` 放进 `<MapView>` 即可（模块自带的演示宿主已经放了）。量算浮层与"已落 N 点"提示由它自己渲染。

```tsx
import { MapView, DrawLayer, mapCommands, useInteraction } from '@map2d'

<MapView data={data}>
  <DrawLayer />
</MapView>
```

**进入/退出绘制**

```ts
mapCommands.setDrawMode('point')          // 落点（单击即完成）
mapCommands.setDrawMode('line')           // 折线 → 默认写入 route（航线）
mapCommands.setDrawMode('area')           // 面   → 默认写入 area（区域）
mapCommands.setDrawMode('measure-line')   // 测距（不落图元）
mapCommands.setDrawMode('measure-area')   // 测面（不落图元）
mapCommands.setDrawMode('none')           // 退出
mapCommands.getDrawMode()                 // → 当前模式
mapCommands.setDrawKind('track')          // 改绘制结果写入哪一类（默认：点→label、线→route、面→area）
```

**绘制时的键鼠约定**

| 操作 | 效果 |
|---|---|
| 单击 | 落一个顶点 |
| 双击 / `Enter` | 完成绘制（面少于 3 点、线少于 2 点会提示并保持绘制态） |
| `Backspace` / `Delete` | 退回上一个顶点 |
| `Esc` | 取消本次绘制（已落顶点丢弃，**未完成的内容不会进图元集合**） |

> 重要语义：绘制过程中的"预览线/预览面/顶点手柄"用独立临时源渲染，**不进 `list()` / `export()`**。
> 只有完成（双击/Enter/落点）后才通过 `MapDraw.add()` 落库，因此导出、显隐、事件命中等能力对绘制结果同样成立。

**图元编辑**

```ts
mapCommands.editPrimitive('area', 'A-1')   // → { ok: true } / { ok: false, reason }
mapCommands.getEditTarget()                // → { kind, id, dragging }
mapCommands.insertVertexAt(0)              // 在第 0 段后插入顶点（取段中点）
mapCommands.removeVertexAt(0)              // 删顶点；区域最少 3 个、折线最少 2 个
mapCommands.finishEdit()                   // 退出编辑态
```

编辑态下顶点会显示为可拖拽手柄（`data` 层），拖动即调用 `MapDraw.add(kind, { ...item, 新顶点 })` 回写——
所以 `list()` 立即可读到最新坐标。

**吸附（M2-DRAW-14）**

```ts
mapCommands.setSnapEnabled(true)   // 默认开启
```

判定阈值是**屏幕 10px**，按当前缩放换算成米；吸附命中时浮层显示"吸附到 {kind}:{id}"，
预览顶点会被替换为已有点坐标。候选点来自所有已有图元的顶点。

**量算（M2-CTRL-10）**

```ts
mapCommands.getMeasurement()
// → { mode:'line', points, meters, bearing } | { mode:'area', points, areaM2, meters } | null
mapCommands.clearMeasurement()
```

**几何计算（宿主可直接调用，不依赖地图）**

```ts
import { distanceMeters, pathLengthMeters, polygonAreaM2, bearingDeg, fmtDistance, fmtArea } from '@map2d'

distanceMeters([116.4, 39.9], [116.5, 39.9])   // → 8517.6（米）
pathLengthMeters(points)                       // 折线总长（米）
polygonAreaM2(ring)                            // 球面多边形面积（m²）
bearingDeg([116.4, 39.9], [116.5, 39.9])       // → 90（正东）
fmtDistance(8517.6)                            // → "8.52 km"
fmtArea(9.47e7)                                // → "94.71 km²"
```

**顶点访问工具**（自定义业务交互时可用）

```ts
verticesOf('area', item)                    // 11 类图元 → 统一顶点数组（区域=环、航线/轨迹=折线、其余=单点）
withVertices('route', item, newPoints)      // 用新顶点生成更新后的图元对象
isEditableShape('area')                     // 顶点可否增删（area/route/track 为 true）
```

**键盘操作（M2-CTRL-14）**：`←→↑↓` 平移（按住 `Shift` 加速 4 倍）、`+/-` 缩放、`Esc` 取消、`Enter` 完成、`Backspace` 退点。
在 `INPUT` / `TEXTAREA` / `SELECT` 内不接管，不影响宿主表单输入。


```ts
import { MapDraw, mapCommands } from '@map2d'

mapCommands.setDrawMode('area')     // 进入绘制模式：'point' | 'line' | 'area' | 'measure' | 'none'
mapCommands.setDrawMode('none')     // 退出（等价于 Esc）

mapCommands.editPrimitive('area', 'A-1')   // 进入编辑态（拖拽整体 / 拖拽顶点）
mapCommands.finishEdit()                    // 结束编辑并写回
mapCommands.setSnap({ toVertices: true, toGrid: false, pixelThreshold: 8 })
```

**约定**：绘制/编辑结果一律经 `MapDraw` 落地（因此可用 `export()` 存档、用 `hide()` 隐藏）；测距/测面结果通过 `MapDraw.on('measure', ...)` 回调给出。

### 10.8 运行指标与错误上报（M2-CTRL-15 / M2-NFR-10）— ✅ 已实现

```ts
import { mapDiagnostics } from '@map2d'

mapDiagnostics.stats()
// → { fps: 58, primitives: { drone: 1200, target: 40, area: 12, ... },
//     tileCache: 180, lastSubmitMs: 3.2, jsHeapMB: 58, degraded: false }

mapDiagnostics.onError((err) => {
  // err = { kind: 'target', id: 'T-?', reason: 'missing lng/lat' }
  console.warn('图元数据非法，已跳过', err)
})
```

**约定**：单条图元数据非法只跳过该条并计数，**不中断整类渲染**（对应 M2-NFR-10 验收：注入一条脏数据后其余图元照常显示）。

### 10.9 视图状态序列化（M2-API-08）— ✅ 已实现（另含图片导出 M2-API-09）

**视图状态序列化（M2-API-08）**

```ts
// 导出：视角 + 显示模式 + 图层开关 + 分组透明度 + 控件开关 + 当前底图 + 精度上限 +（默认）图元
const state = await mapCommands.exportViewState()
// → { version:1, at, view:{lng,lat,zoom,bearing}, displayMode, activeTool,
//     layers:{...}, layerOpacity:{...}, controls:{...}, basemap, tileMaxMetersPerPixel, primitives }

// 只要"视图"不要图元（例如只想记住用户看到哪儿了）
const viewOnly = await mapCommands.exportViewState({ withPrimitives: false })

// 恢复：传整个快照或其子集
await mapCommands.restoreViewState(state, { duration: 600 })
// → { ok:true, applied:['basemap=road','tileMax=null','layers','layerOpacity','controls=compass,legend','view','primitives'] }
```

**恢复顺序是有讲究的**（模块内部固定）：底图 → 精度上限 → 图层开关/透明度 → 控件 → 视角 → 图元。
原因是底图切换会**重建整个样式**，反过来做的话前面设好的图层状态与视角会被冲掉。

`applied` 数组说明每一项是否真的落地了；底图不存在时不会中断，而是记为 `basemap 跳过（原因）` 继续恢复其余部分。

**图片导出（M2-API-09）**

```ts
const dataUrl = await mapCommands.exportImage({
  type: 'image/png',      // 或 'image/jpeg'
  quality: 0.92,          // JPEG 质量
  withControls: true,     // 是否把已开启的控件（指北针/经纬度/图例/比例尺）合成进去
  background: '#0a1420',  // 底图未加载区域的底色
})

await mapCommands.downloadImage('态势图.png')   // 直接触发下载，返回 dataURL
```

> 导出依赖 WebGL 保留缓冲区：模块默认已开 `preserveDrawingBuffer: true`；
> 若宿主自建 Map 实例，需要自己开，否则可能取到全黑图。
> 控件合成是"底色 + 位置"的简化合成（不引 html2canvas），画布内容为像素级原样。

**渲染时机计数（M2-NFR-14）**

```ts
mapCommands.getRenderTiming()
// → { writes, renders, coalesced, writesPerSec, rendersPerSec }
mapCommands.getStats().render   // 同一份数据（在运行指标里）
```

口径：`writes` 是绘制 API 的写入调用次数（含 `batch` 内部的每次调用），
`renders` 是真正落到地图数据源的次数（`setData`）。
`batch()` 内 50 次写入实测为 **1 次渲染**；非批量写入是 1:1（语义明确，不做隐式合并）。


```ts
const state = mapCommands.exportViewState()
// → { center, zoom, bearing, displayMode, basemapId, layers: { hidden: ['trail'] },
//     controls: ['compass', 'scale'], tilePrecision: { maxMetersPerPixel: 10 } | null }

mapCommands.applyViewState(state)     // 恢复：视角 / 显示模式 / 底图 / 图层开关 / 控件 / 精度上限
```

**约定**：导出内容只包含"地图自身状态"，不含宿主业务数据；恢复后画面应与导出时一致。

---

## 11. 接入到宿主（主系统示例）

```ts
// vite.config.ts —— 别名指向模块源码
alias: { '@map2d': fileURLToPath(new URL('../map-2d/src/index.ts', import.meta.url)) }
// tsconfig.json
paths: { "@map2d": ["../map-2d/src/index.ts"] }, include: ["../map-2d/src"]
```

```tsx
// 唯一适配器：业务 store → MapData（换数据源只改这一个文件）
const mapData = useMapData()
<MapView data={mapData}>
  <MapToolbar />
  <LayerPanel />
  <Compass />
  <MapModeBadge mode={displayModeOf(scenarioKey, phase)} />
</MapView>
```

**边界约定**

- 宿主不得出现 `maplibre-gl` 直接调用与模块内部路径；
- 宿主业务状态不进模块；模块 UI 状态不与宿主 store 混用；
- 需要"定位到某目标""清屏""切底图"等动作时，调用本模块接口，而不是自己操作 DOM。

---

## 12. 错误与降级约定

| 场景 | 模块行为 | 宿主应对 |
|---|---|---|
| 地图未就绪就调命令 | `mapCommands` 静默返回；`isReady()` 为 false | 轮询 `isReady()` 或等 `data.config` 到位 |
| 瓦片目录缺失 / 服务未起 | 回落纯色底，页面与交互正常；控制台按累计次数告警 | 无需特殊处理；如需提示可自行监听控制台 |
| 单张瓦片 404 | 该处显示缺口底色，不隐藏整层 | 无 |
| 图元数据非法（缺 id/坐标） | 当前实现可能整类渲染异常；**错误边界已设计待实现**（§10.8） | 传入前做最小校验 |
| 未配置 `config` | 使用内置默认视角（北京 z11），不报错 | 无 |

---

## 13. 接口速查表

| 我想…… | 用 |
|---|---|
| 显示地图 | `<MapView data={mapData}>` |
| 画区域/目标/无人机/链路/轨迹/扫描/脉冲/集群/标注 | `MapDraw.set/add/remove/clear` |
| 存档与恢复图上内容 | `MapDraw.export()` / `MapDraw.load()` |
| 移动镜头、定位、复位 | `mapCommands.setView/flyTo/focus/resetView/zoomBy` |
| 读当前视口 | `mapCommands.getViewport()` |
| 判断地图是否可用 | `mapCommands.isReady()` |
| 开关某类图层 | `<LayerPanel />` 或 `LayerManager.setGroupVisible` |
| 清屏（只留地图） | `useMapUiStore.toggleClearMode()` + 宿主自行隐藏面板 |
| 显示模式徽标 | `<MapModeBadge mode={displayModeOf(scenarioKey, phase)} />` |
| 指北针 | `<Compass />` |
| 批量灌业务数据 | `LayerManager.setTargets/setGroups/setUavs/setLinks/setTrack` |
| 显示/隐藏版权署名 | `MAP_OPTIONS.showAttribution` |
| 调整瓦片淡入 | `MAP_OPTIONS.rasterFadeDuration` |
| **（待实现）** 显示/隐藏指北针、经纬度、缩放按钮、比例尺 | §10.1 `mapCommands.showControls` |
| **（待实现）** 单个图元显隐、批量提交 | §10.2 `MapDraw.hide/show`、`MapDraw.batch` |
| **（待实现）** 多套底图枚举与切换 | §10.3 `basemaps.list/switch/onChange` |
| **（待实现）** 限制地图只看某个精度 | §10.4 `mapCommands.setTilePrecisionLimit` |
| **（待实现）** 轨迹回放与时间轴 | §10.5 `replay.load/play/seek` |
| **（待实现）** 图元点击/悬停回调 | §10.6 `MapDraw.on` |
| **（待实现）** 手绘与图元编辑 | §10.7 `mapCommands.setDrawMode` |
| **（待实现）** 读运行指标 / 错误上报 | §10.8 `mapDiagnostics` |
