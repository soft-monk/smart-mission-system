# 智能任务管理系统 · 界面与流程需求初稿

## 1. 文档说明

| 项目 | 说明 |
|------|------|
| 文档名称 | 智能任务管理系统 · 界面与流程需求初稿 |
| 素材来源 | 本仓库 `docs/01-需求/需求图与描述/`（原 `C:\Users\softmonk\Desktop\9.4界面图片和文字`） |
| 素材内容 | 3 个任务场景的文字描述（Word）+ 对应界面原型图（PNG）：系统启动界面一/二、系统 Logo、场景 1/2/3 各 T0–T7×2 张、独立态势图（第二屏/第三屏） |
| 文档定位 | 根据现有界面图片与文字描述整理的产品/界面初稿，用于后续 PRD、设计评审与研发排期讨论 |
| 当前状态 | 初稿，待补充业务规则、数据口径、权限规则、埋点需求及排期 |
| 信息提取口径 | 界面图与文字稿均为需求来源。二者冲突时以**界面图为准**；各场景章开头标注该章的提取方式与全局版式，个别条目以 `【文字稿】` 标注「仅文字稿有而界面图未体现」的内容；图中数值（架数、百分比、坐标、时间、编号）均为演示数据，不构成指标要求 |

## 2. 产品概述

### 2.1 系统定位

**智能任务管理系统**是一款面向任务指挥与无人集群协同的智能化平板端系统，集战场/任务态势感知、AI 任务分析、无人集群编组、通信链路自组网、多源侦察融合、目标识别、任务决策、协同打击/处置、毁伤/效果评估于一体。

系统强调：
- **AI 语音交互**：全程支持“系统提示—用户语音—AI 回复”的免手操作。
- **人在回路决策**：AI 生成推荐方案，最终由用户确认/调整。
- **多域协同**：光电、雷达、电子、通信四类无人机集群与战术小组、后方火力/云端算力协同。

### 2.2 三大任务模式

三张任务方向卡在各场景 T0-1 中并排出现，当前场景卡高亮并带「推荐任务方向」角标。

| 场景 | 模式名称 | 核心特征（界面口径） | 典型场景（T0 左侧「区域范围」） |
|------|----------|----------------------|--------------------------------|
| 场景一 | 敏捷拒止布控 | 前沿局部组网、侦察覆盖、电子压制、固定通信受限下自主运行 | 山区/城镇/交通节点混合区域 |
| 场景二 | 集群协同攻击 | 云边端协同、边缘自治与断链自主运行、多集群并行执行 | 沿海重点方向 |
| 场景三 | 立体融合攻坚 | 建筑群内部空间感知、多层结构、战术小组与无人集群协同搜索处置 | 建筑群区域 |

### 2.3 核心流程概览（T0–T7）

所有场景共用同一套阶段框架，仅在 T1/T2/T3/T5/T6 等阶段的业务重点和组件呈现上根据模式差异化：

1. **T0** 任务加载与场景选择  
2. **T1** 无人机/资源分组与任务编组  
3. **T2** 通信链路/云边协同/建筑空间感知建立  
4. **T3** 多源侦察展开与态势构建 / 目标搜索  
5. **T4** 目标识别、评估与优先级筛选  
6. **T5** 任务决策与打击/处置准备  
7. **T6** 协同执行与引导  
8. **T7** 毁伤/效果评估与任务结束

---

## 3. 通用界面与交互框架

### 3.1 视觉风格

- **整体色调**：深色科技风，以深蓝/近黑为底，搭配青色、亮蓝、绿色作为信息高亮；红色用于威胁/高价值目标，黄色用于弱链路/待复查区域。
- **质感**：半透明毛玻璃卡片、霓虹描边、微光效果、数据可视化图表。
- **字体**：无衬线字体，标题加粗，数据指标大字号突出。

### 3.2 全局布局（平板横屏）

```
┌─────────────────────────────────────────────────────────────┐
│  [Logo] 智能任务管理系统          [网络] [WiFi] [电量] [时间] [用户] │  ← 顶部状态栏
├──────┬──────────────────────────────────────────────┬───────┤
│      │                                              │       │
│  侧  │           中央三维/二维态势地图               │  右侧 │
│  边  │           （任务区域、集群轨迹、目标标记）     │  AI/  │
│  导  │                                              │  状态 │
│  航  │                                              │  面板 │
│      │                                              │       │
├──────┴──────────────────────────────────────────────┴───────┤
│  [系统状态] [无人机状态] [数据链路] [定位] [AI决策引擎] [安全]      │  ← 底部全局状态条
│                                                    (🎤 语音按钮) │
└─────────────────────────────────────────────────────────────┘
```

**全局元素出现范围（据界面图实测，需在设计中统一）**：
- 顶部状态栏：全部界面均有（Logo + 系统名；网络：已连接、WiFi、电量、时间、用户头像）。
- 左侧功能导航：场景一 T0–T7 与场景二 T0–T7 均有；**启动阶段两张图与两张独立态势图无**。
- 底部全局状态条：场景一 T0/T1 等界面有；场景二仅 T4-1 出现。
- 右下角麦克风语音按钮：场景一 T0–T7 有；**场景二 16 张图均无**，语音改以页面内「AI 自动语音 / AI 提示 / 系统语音 / 用户语音 / 系统回复」对话行呈现。
- 三处不一致宜在设计阶段统一（见第 11 章）。

### 3.3 左侧导航

固定入口（图标 + 文字）：
- 态势
- 任务
- 目标
- 区域
- 资源
- 告警
- 设置

当前选中项高亮显示。

### 3.4 顶部状态栏

- 左侧：系统 Logo + 系统名称
- 右侧：网络连接状态、WiFi 信号、电量、系统时间、用户头像

### 3.5 底部状态栏

- 系统状态：运行正常 / 异常
- 无人机状态：全部正常 / 部分异常
- 数据链路：稳定 / 波动 / 受限
- 定位状态：正常
- AI 决策引擎：在线 / 离线
- 安全状态：安全
- 右下角语音交互按钮（麦克风悬浮球）

### 3.6 地图操作控件

- 工具组（随阶段变化）：选择、测距、图层、区域、新建、全屏、标绘、2D/3D 切换
- 缩放（+/-）、指北针
- 比例尺
- 显示模式下拉（右上角），随阶段切换，已见取值：
  - 综合态势（T0/T1）
  - 侦察展开、侦察融合（T3）
  - 目标识别（T4）
  - 任务规划、打击确认（T5）
  - 实时态势、引导控制（T6）
  - 复核态势、结果汇总（T7）

### 3.7 通用卡片组件

| 组件 | 用途 | 典型内容 |
|------|------|----------|
| 场景入口卡片 | T0 模式选择 | 场景名称、副标题、推荐标签、播放/进入按钮 |
| 集群编组卡片 | T1 | 集群名称、四型无人机数量、任务方向、覆盖区域 |
| 方案对比卡片 | T1/T5 | 方案名称、打击方式、参与集群、成功率、打击效果、推荐标签 |
| 目标列表卡片 | T4 | 目标编号、类型、威胁等级、坐标、来源 |
| 目标详情卡片 | T4 | 目标类型推测、活动状态、威胁等级、打击窗口 |
| 链路拓扑卡片 | T2 | 节点连接图、链路质量、带宽/延迟/丢包 |
| 任务流程进度条 | 全局 | 任务下达 → 方案生成 → 资源调度 → 区域搜索 → 目标确认 → 任务完成 |
| AI 语音交互面板 | 全局 | 系统说 / 用户说 / 系统回复 |
| AI 提示/建议卡片 | 全局 | 单条 AI 提示或建议（如「目标区域出现移动迹象，建议调整观察角度」） |
| 状态指标条 | 全局 | 名称 + 数值 + 状态色（跟踪稳定度、命中概率、回收进度等） |
| 集群状态列表 | T3/T4 | 各集群类型、在线状态、信号强度、电量 |
| 报告生成面板 | T7 | 目标摧毁/处置数量、覆盖率、协同效率、链路稳定性 |

### 3.8 AI 语音交互范式

系统多处采用“三段式”语音交互：
1. **AI 自动提示**：进入界面或检测到关键事件时主动播报。
2. **用户语音指令**：用户通过麦克风发出简短指令。
3. **AI 回复**：系统结合态势给出结论并触发界面变化。

---

## 4. 系统启动阶段（T0 之前）

### 4.1 启动加载界面（系统启动界面一）

**触发**：平板上电后自动进入。

**界面元素**：
- 中央：系统 Logo + 系统名「智能任务管理系统」+ 副标题「系统启动中，请稍候…」+ 总进度条与百分比。
- 下方：模块加载进度卡片（每项带图标、名称与百分比）
  - 地图引擎
  - 通信链路
  - AI 引擎
  - 集群管理
  - 数据服务
- 右侧：系统状态卡，四项各显示图标 + 名称 + 状态文字
  - 网络连接（在线）
  - 数据链路（稳定）
  - GPS 定位（正常）
  - 系统安全（安全）
- 顶部：系统 Logo + 系统名；右侧网络已连接、WiFi、电量、时间、用户头像。
- 底部：提示条「系统启动中，正在初始化核心模块…」。
- 未出现左侧功能导航与底部全局状态条（启动阶段独立版式）。

### 4.2 语音引导校验界面（系统启动界面二）

**触发**：加载完成后自动弹出。

**流程**：
1. AI 语音提示：“请进行系统状态确认”。
2. 左侧标题「系统状态自检」+ 副标题「请确认系统状态，确保各模块正常运行」+ Logo 动效，下方【一键自检】按钮；底部提示「自检过程预计耗时 15~30 秒」。
3. 点击【一键自检】后展开「自检结果」分项检测卡片，每项含图标、名称、子说明、状态文字与通过标记：
   - 通信链路检测（卫星链路 / 数传链路 / 组网链路）→ 正常
   - 定位系统检测（GPS / 北斗定位服务）→ 正常
   - 集群节点检测（集群节点状态 / 设备在线率）→ 正常
   - 后方指控检测（指控平台连接 / 数据交互服务）→ 正常
   - 系统安全检测（系统完整性 / 安全防护机制）→ 正常
4. 右侧「系统状态概览」同步显示网络连接、数据链路、GPS 定位、系统安全。
5. 自检通过后：
   - 显示【重新检测】与【进入任务】按钮；
   - 点击【进入任务】进入 T0。
6. 异常分支（自检不通过时的处理流程）待确认，见第 11 章。

---

## 5. 任务主流程 T0–T7（通用框架）

| 阶段 | 核心目标 | 关键界面 | 关键动作 |
|------|----------|----------|----------|
| T0 | 选择任务模式 | 任务态势主界面、场景确认界面 | AI 推荐场景、用户确认 |
| T1 | 资源编组 | 集群编组界面、方案确认界面 | 生成三套方案、AI 推荐、确认编组 |
| T2 | 建立链路/感知 | 链路拓扑/云边拓扑/建筑空间态势 | 自组网、链路优化、边缘自治激活 |
| T3 | 侦察/搜索 | 侦察展开地图、数据融合界面 | 多源侦察、热点标记、态势构建 |
| T4 | 目标识别评估 | 目标列表、目标详情 | 目标关联、威胁评级、优先级排序 |
| T5 | 任务决策 | AI 决策界面、打击/执行确认界面 | 生成方案、选择最优、生成任务 |
| T6 | 协同执行 | 执行态势、引导控制 | 实时修正、轨迹更新、命中引导 |
| T7 | 效果评估 | 毁伤评估地图、任务总结 | 状态变化、报告生成、任务结束 |

---
## 6. 场景一：敏捷拒止布控

> 推荐场景：山区/城镇/交通节点混合区域，固定通信受限，敌方纵深防御明显。
> 界面证据：`需求图与描述/场景1/`（系统启动界面一/二、T0–T7 各 2 张）
> 说明：本章按界面图逐屏提取。文字稿与界面图存在出入处，以界面图为准并加注 `【图】`；仅文字稿有、图中未体现的内容加注 `【文字稿】`。

### T0｜任务加载与场景选择

**T0-1 任务态势主界面**：
- 顶部：系统名 + 网络状态、WiFi、电量、时间。
- 中央：三维地形态势地图，标注 A 区域（蓝）、B 区域（绿）、C 区域（红）、敌方潜在部署区、我方预备集群 1/2/3；含比例尺（0–15 km）、缩放、指北针、显示模式下拉、2D/3D 切换、选择/新建/全屏/区域 四个地图工具。
- 右侧三块面板：
  - AI 任务分析：当前区域威胁等级（高）、敌方防御强度（中）、通信干扰强度（低）、态势简述、建议方向。
  - 任务信息：任务名称、任务类型、任务区域、任务时间、任务状态。
  - 资源概况：光电/雷达/电子/通信四型无人机数量、集群可用、在线率。
- 底部：三大场景入口卡（场景一 敏捷拒止布控 / 场景二 集群协同突击 / 场景三 立体融合攻坚），各带缩略图与进入按钮。

**T0-2 场景确认界面**：
- 中央地图切换为友方集结区（A）、监视区（B）、高价值目标区（C）、敌方潜在部署区。
- 右侧 AI 任务分析（当前风险等级、敌方威胁强度、通信干扰强度 + AI 分析结果条目）、任务信息。
- 右侧底部按钮：【进入任务执行】、【返回场景选择】。
- AI 语音：播报战场态势建模结果，说明固定通信受限、敌方纵深防御特征，建议构建前沿局部任务体系。
- 用户语音：“推荐一个任务模式”；AI 回复推荐【敏捷拒止布控】及理由。
- 中央：任务目标说明卡 + AI 分析（敌情态势摘要、推荐作战方式、前沿部署建议）；底部原按钮为【确认进入任务】、【重新选择】`【文字稿】`。

### T1｜无人机分组与任务编组

**T1-1 集群编组界面**：
- 左侧无人机类型列表（四类）：

| 类型 | 总数量 | 可用数量 | 任务能力标签 | 当前分配 | 待分配 |
|------|--------|----------|--------------|----------|--------|
| 光电无人机 | 48 | 48 | 侦察 / 识别 / 跟踪 | 已分配 34 | 待分配 14 |
| 雷达无人机 | 32 | 32 | 探测 / 扫描 / 跟踪 | 已分配 20 | 待分配 12 |
| 电子无人机 | 28 | 28 | 干扰 / 压制 / 侦察 | 已分配 18 | 待分配 10 |
| 通信无人机 | 24 | 24 | 中继 / 通信 / 数据 | 已分配 16 | 待分配 8 |

- 中央方案生成区（显示模式：综合态势）：三套方案并排，推荐方案高亮描边。
  - 方案一｜稳态侦察覆盖方案：5 个集群（侦察集群 1、侦察集群 2、通信中继集群、电子侦察集群、预备支援集群）。
  - 方案二｜多域协同压制方案【AI 推荐】：6 个集群（前出侦察集群、侧翼侦察集群、雷达探测集群、通信中继集群、电子压制集群、机动预备集群）。
  - 方案三｜重点区域突破方案：4 个集群（重点侦察集群、目标跟踪集群、通信保障集群、电子干扰集群）。
  - 每个集群框显示：光电/雷达/电子/通信四型配比，以及任务方向、覆盖区域/协同关系。
- 右侧 AI 推荐编组方案：推荐方案与推荐评分（93%）；推荐理由 4 条；预期效果 6 项（区域覆盖率、链路稳定度、目标发现效率、电子压制能力、任务成功率、资源利用率）；系统状态 4 条（自动完成资源注册与能力识别、完成百架级资源分配、生成三套任务编组方案、推荐方案已高亮显示）。
- 底部按钮：【查看方案详情】、【自动优化】、【确认采用推荐方案】。
- 底部信息条：当前编组资源 124/132、待分配资源、资源利用率 86%；资源状态（在线数）、集群数量（已编组数）、链路就绪率、AI 状态。

**T1-2 方案确认界面**：
- 中央：地图上为方案二每个集群圈定并编号任务区（1 前出侦察集群、2 侧翼侦察集群、3 雷达探测集群、4 通信中继集群、5 电子压制集群、6 机动预备集群），标注前沿指挥节点。
- 每个集群卡显示：架构组成（四型无人机数量 + 各型百分比）、任务属性（如远程侦察/目标发现、侧翼侦察/威胁感知、通信中继/链路保障、电子压制/干扰压制、机动待命/快速支援）、协同关系（直接上报、数据共享、信息支撑、链路保障、掩护保障、快速响应）。
- 左侧方案资源总览：当前方案、集群总数、参与资源总数、资源就绪率；集群列表（各集群资源数与就绪百分比）。
- 右侧方案确认信息：当前方案（已选择并确认）、任务区域覆盖率、链路就绪率（含链路状态）、协同效率评估（含协同等级）、方案优势 4 条、方案备注。
- 底部：【确认编组】、【自动优化】。

### T2｜通信链路建立

**T2-1 链路拓扑界面**：
- 中央：通信拓扑图，6 个集群节点（前出侦察、侧翼侦察、雷达探测、通信中继、电子压制、机动预备）网状互联，并接入前沿指挥节点；链路状态绿=已连接、黄=弱链路。
- 右侧 AI 链路分析：推荐链路组织方式（当前网络稳定；支持多链路协同；链路延迟满足实时控制要求）；网络评估 4 项（链路稳定度、覆盖率、协同效率、抗干扰能力）；链路质量指标（信号强度、带宽、时延、丢包率、覆盖范围、局部组网进度）。
- 底部：【自动优化链路】、【进入侦察阶段】；底部信息条同 T1-1（资源状态、集群数量、链路就绪率、AI 状态）。
- 左侧链路质量指标、局部组网进度、体系接入状态 `【文字稿】`。

**T2-2 链路稳定界面**：
- 点【自动优化链路】后，右侧切换为链路稳定评估：当前网络稳定 / 前沿局部网络已建立；带宽提升曲线、时延降低曲线（带时间轴）；网络评估 4 项数值更新；链路稳定度、覆盖率、协同效率、抗干扰能力。
- AI 语音：“通信链路已完成自组网，当前链路质量达到任务标准。前沿节点已具备独立通信与感知支撑能力。”
- 用户语音：“链路是否支持多集群协同？”；AI 回复支持三集群并行数据回传与协同控制、延迟满足实时控制要求。
- 底部：【自动优化链路】、【进入侦察阶段】。

### T3｜光电侦察展开

**T3-1 侦察展开地图界面**：
- 显示模式切为「侦察展开」，地图工具为 选择/测距/图层/2D-3D。
- 中央：地图上按编号标注集群 1–6（含各自着色的覆盖范围），集群 2 标注前出箭头，集群 6 标注重点区域。
- 右侧集群总体状态：集群 1–6 逐条显示在线状态、信号强度（强/中/弱）、电量；集群类型以**颜色区分**并在地图与列表间一一对应（图中：蓝=集群 1 光电、绿=集群 2 光电、黄=集群 3 雷达、紫=集群 4 电子、青=集群 5 通信、橙=集群 6 通信），列表中另以彩色圆点复现该映射。`【文字稿提示】`原文字稿称此面板「显示各集群类型」，但图中**未出现类型文字**，仅以颜色编码；若要求显式可读，需补充类型文字。
- 底部：地图工具栏、【更多详情】。

**T3-2 侦察数据融合界面**：
- 显示模式「侦察融合」。
- 中央：六路集群图像流（集群 1 光电、集群 2 光电、集群 3 雷达、集群 4 电子、集群 5 通信、集群 6 通信），底部数据源栏（光电 / 雷达 / 电子 / 通用）。
- 右侧：集群总体状态同 T3-1。
- 多源数据实时回传前沿指挥节点 `【文字稿】`。
- AI 语音：“检测到异常电磁信号聚集区域，已标记为潜在目标区……”；地图自动高亮热点区域，同步显示光电图像、雷达回波、电子信号来源标识。

### T4｜目标识别与筛选

**T4-1 目标列表界面**：
- 显示模式「目标识别」。
- 目标列表：5 个目标缩略图卡（目标 001 高、目标 002 高、目标 003 中、目标 004 中、目标 005 低），高价值目标红色标记，卡可左右滚动。
- 点击目标后地图定位高亮（带十字准星与判定框），右侧显示目标详情：目标类型、置信度、位置（经纬度）、海拔、目标特征（大型建筑、卫星天线、通信设备、车辆活动）、威胁评估（威胁等级高、防护强度强、电子干扰中、周边态势可控）。
- 来源标识：光电/雷达/电子。
- 目标类型：机动指挥节点、防空火力阵地、通信保障节点。

**T4-2 目标详情界面**：
- 右侧目标详情 + AI 分析结果：类型判断、行为特征、威胁等级、活动状态、建议处置。
- 底部动作按钮：【升级为打击目标】、【交由集群重点监视】、【持续跟踪】。
- 用户语音：“哪个目标优先级最高？”；AI 回复推荐高价值节点（指挥+通信双重功能）并建议优先纳入打击序列。
- 目标编号一致性：文字稿称「目标 003 为高价值节点」，界面图中高价值红标为**目标 002**，属演示数据差异，需求以「高价值目标红色标记 + 可语音询问优先级」为准。

### T5｜任务决策与打击准备

**T5-1 AI 决策界面**：
- 显示模式「任务规划」。
- 右侧 AI 语音交互面板：系统说（基于目标价值、暴露时间窗口与链路稳定性已生成三种打击方案，可同步生成目标信息包并推送至后方远程火力与空中支援力量）；用户说“选择最优方案”；系统回复（已选择方案二：多集群协同压制……）。
- 中央：打击方案卡 3 张，各显示打击方式、参与集群、成功率预估、打击效果：

| 方案 | 打击方式 | 参与集群 | 成功率预估 | 打击效果 |
|------|----------|----------|------------|----------|
| 方案一 光电精确打击 | 精确打击 | 集群 2（光电） | 68% | 精准摧毁 |
| 方案二 多集群协同压制【推荐方案】 | 协同压制 | 集群 1/2/3/4/5/6 | 82% | 压制摧毁 |
| 方案三 电子干扰配合 | 电子干扰+打击 | 集群 4（电子）+集群 5（通信） | 61% | 干扰瘫痪后打击 |

**T5-2 打击确认界面**：
- 显示模式「打击确认」。
- 左侧目标清单：目标 003 指挥通信节点（高价值，优先级 1）、目标 001 防空火力阵地（中高威胁，优先级 2）、目标 002 通信保障节点（中威胁，优先级 3）。
- 中部打击窗口时间轴：T+00 信息包生成 → T+03 火力协同确认 → T+06 空中支援接入 → T+09 等待任务生成；含窗口剩余倒计时。
- 右侧：资源调度情况（光电集群持续跟踪、雷达集群位置校正、电子集群压制待命、通信集群链路中继）；信息包与协同状态（目标信息包生成状态、远程火力协同状态、空中支援接入状态、链路稳定性）；AI 确认建议。
- 底部：【生成打击任务】、【调整方案】。

### T6｜协同执行与引导

**T6-1 执行态势界面**：
- 显示模式「实时态势」。
- 中央：集群轨迹动态运行、目标锁定框（目标 003）、多源数据叠加、火力协同链路。
- 右侧：AI 提示（目标区域出现移动迹象，建议调整光电集群观察角度）；跟踪状态（目标跟踪稳定度、位置校正状态）；协同链路（火力协同链路、雷达数据、电子侦察）。
- 底部状态条：多源数据叠加、实时锁定、轨迹修正；按钮【调整观察角度】、【保持跟踪】。

**T6-2 引导控制界面**：
- 显示模式「引导控制」。
- 中央：AI 轨迹修正示意，目标 003 位置更新，偏差标注（偏差 12 m）、信息同步链路。
- 右侧：AI 轨迹修正说明；命中概率（含趋势）；同步状态（目标位置实时更新、火力引导信息已同步、偏差修正已生成）。
- 底部状态条：轨迹已修正、目标锁定、信息同步；按钮【确认引导】、【重新规划】。
- AI 实时修正轨迹、偏差修正建议 `【文字稿】`。

### T7｜毁伤评估与任务结束

**T7-1 毁伤评估地图界面**：
- 显示模式「复核态势」。
- 中央：目标状态变化（红 → 黄 → 灰，图中显示目标 003 已失效、已清除），残余威胁标记 2 处、集群回收轨迹、复核航线（虚线）、节点待脱离。
- 右侧：目标状态（目标、区域状态、残余风险）；集群回收（回收进度、返航集群数）；节点状态（前沿节点、复核侦察、链路释放）。
- 底部：【继续复核】、【准备脱离】。

**T7-2 任务总结界面**：
- 显示模式「结果汇总」。
- 中央结果卡：目标清除 3、覆盖区域 24.6 km²、协同效率 91%；报告生成进度（已完成）；信息包已推送、残余风险低、链路待释放。
- 右侧：AI 摘要（主要威胁节点已清除，建议保持区域持续监视）；关键结果（成功率、组网时长、推送结果）；后续处置（资源撤收、链路释放、部署转移）。
- 底部：【返回场景选择】、【导出任务报告】、【快速脱离体系】。
- AI 语音：总结打击成功率、威胁清除情况、后续监视建议；自动生成报告（目标摧毁数量、覆盖区域、集群协同效率、局部组网持续时长）。

---

## 7. 独立态势图（大屏／全屏态势）

`需求图与描述/` 根目录另有两张独立态势图，版式与 T0–T7 平板界面不同（**无左侧功能导航、无底部全局状态条、无右下角语音按钮**），为面向大屏/全屏的独立态势展示界面，标题栏统一为「智能任务管理系统 - <态势名>」，右上角显示系统时间与任务状态。

### 7.1 第二屏｜任务执行态势

- 左侧「当前任务」：任务名称（重点区域侦察）、任务阶段（搜索执行中）、完成进度。
- 中央三维地形态势：范围内标注**核心搜索区**、**西侧重点侦察区**、**北侧重点侦察区**；我方 4 架无人机图标分别带光电/雷达/电子/通信标识，虚线为航线/搜索轨迹；标注**战术小组**位置；图例为「当前搜索区域 / 重点侦察区域 / 已覆盖区域」；含已覆盖边界的半透明面。
- 右侧「无人机资源状态」：光电侦察、雷达侦察、电子侦察、通信中继各架数（图中各 4 架）。
- 右侧「链路状态」：光电链路、雷达链路、电子链路、通信链路，各带信号格与百分比。
- 底部「任务执行流程」时间轴：任务下达 → 方案生成 → 资源调度 → 区域搜索（执行中）→ 目标确认（待执行）→ 任务完成（待执行），已完成节点带时间戳与对勾。

### 7.2 第三屏｜红方侦察态势

- 左侧「信息汇聚来源」：侦察集群 1–4（各显示发现目标数）+ 历史情报关联（关联目标数），中间以「多源信息汇聚」箭头指向态势图。
- 中央「红方综合态势图」：地图上标注各类节点（通信节点、指挥节点、火力阵地、装甲目标、集结区域、后勤保障节点），节点分**已确认**与**疑似**（虚线框 + 问号图标）两态；含威胁等级图例（高/中/低）、侦察集群标注（发现目标数）。
- 右下「态势统计」：已确认目标、疑似目标、关联目标、高威胁区域数量。
- 右侧「AI 态势研判」：发现疑似指挥节点（置信度、关联目标数）；发现目标集结趋势（预计时间后向东侧区域转移）；建议下一步行动（加强某方向侦察）。
- 底部「情报关联关系图」：以箭头串联的关联链（通信节点 → 指挥节点 → 火力阵地 → 装甲目标 → 集结区域 → 后勤保障节点）。

## 8. 场景二：集群协同攻击

> 推荐场景：沿海重点方向，云边端链路已建立，多节点协同执行条件具备，强调边缘自治与动态优化。
> 界面证据：`需求图与描述/场景2/`（T0–T7 各 2 张）
> 说明：本章按界面图逐屏提取并逐字抄录图中字段。图中数值为演示数据。
> 全局版式：16 张图均含左侧功能导航（T0-1 高亮【态势】，其余高亮【任务】）与顶部状态栏；**无右下角语音按钮**，语音以「AI 自动语音 / AI 提示 / 系统语音 / 用户语音 / 系统回复」对话行内嵌呈现；底部全局状态条仅 T4-1 出现。

### T0｜任务加载与场景选择

**T0-1 任务态势主界面**：
- 中央：三维地形海岸底图（海上舰船图标、内陆设施图标，含地名标注）；地图工具条【选择】【新建】【全屏】【区域】【3D 2D/3D】，指北针 N，缩放 +/−。
- 左侧三项：任务来源（上级指派）、区域范围（沿海重点方向）、前沿节点状态（已接入 / 正常）。
- 右侧「AI任务分析」：风险等级（中等）、建议方向（集群协同攻击）、云端接入状态（已建立）；「系统运行状态」：网络连接（在线）、数据链路（稳定）、系统就绪度（就绪）。
- 底部三张任务方向卡：敏捷拒止布控 / **集群协同攻击**（高亮，角标「推荐任务方向」）/ 立体融合攻坚。

**T0-2 场景确认界面**：
- 顶部三条对话行：
  - AI 自动语音：“已完成战场态势建模，当前区域具备多节点协同执行条件，云边端链路已建立，具备自主任务执行基础。”
  - 用户语音：“推荐任务模式”。
  - 系统回复：“……建议采用【集群协同攻击】模式，实现云边端一体化自主执行。”
- 右侧能力状态：多节点协同（具备）、云边端链路（已建立）、自主执行基础（已就绪）。
- 中央任务模式说明卡（标题「集群协同攻击」+ 标签「推荐模式」）：态势融合结果、任务协同结构、资源调度建议各一条结论文字。
- 底部：【确认进入任务】、【重新选择】。

### T1｜无人机分组与任务编组

**T1-1 集群编组方案界面**：
- 左栏「无人机类型」（图标 + 名称 +「可用」+ 数量）：光电无人机 22、雷达无人机 12、电子无人机 10、通信无人机 8。
- 中栏「集群编组方案」三张方案卡：
  1. 方案一｜分布式稳态感知（持续态势回传 / 稳定侦察覆盖）。
  2. 方案二｜云边协同自适应攻击【AI推荐】：6 个集群——前出侦察集群、侧向感知集群、雷达探测集群、边缘协同处理集群、电子对抗集群、机动执行集群；卡内显示四型合计（光电 22 / 雷达 12 / 电子 10 / 通信 8）、任务方向（集群协同攻击）、云边协同（已联动）。
  3. 方案三｜集中式快速压制（局部集中调度 / 快速压制目标）。
- 右侧三块：AI 推荐方案（方案二｜云边协同自适应攻击）；推荐理由 4 条（边缘节点支持实时融合 / 云端提供全局优化 / 前沿集群可自主执行 / 适配多目标动态变化）；预期效果 4 项（态势更新时效、决策响应速度、资源调度效率、协同稳定性）；系统状态（云边端链路 已建立、编组方案 已生成、推荐方案 已高亮）。
- 底部：【查看方案详情】、【自动优化】、【确认采用推荐方案】。
- 注：逐集群四型数量不在本界面，见 T1-2。

**T1-2 方案确认界面**：
- 中央：顶部「云端协同中心」，下方 6 个集群框（各带边缘节点图标，以青色虚线回传链路连至云端协同中心，并在地图上形成区域划分多边形）；左下角图例：云端 / 边缘节点 / 数据回传链路。
- 逐集群四型数量（光电/雷达/电子/通信）：

| 集群 | 光电 | 雷达 | 电子 | 通信 |
|------|------|------|------|------|
| 前出侦察集群 | 6 | 3 | 2 | 1 |
| 侧向感知集群 | 5 | 2 | 2 | 1 |
| 雷达探测集群 | 0 | 5 | 1 | 1 |
| 边缘协同处理集群 | 3 | 1 | 2 | 2 |
| 电子对抗集群 | 1 | 0 | 3 | 1 |
| 机动执行集群 | 7 | 1 | 0 | 2 |

- 右侧：编组方案（方案二｜云边协同自适应攻击）；总量概览（光电 22 / 雷达 12 / 电子 10 / 通信 8）；云边协同（已联动）、边缘节点（已接入）、数据回传（稳定）；「任务区域已完成划分」。
- 底部：【确认编组】、【自动优化】。

### T2｜云边通信与协同链路建立

**T2-1 云边端拓扑构建界面**：
- 中央：三层云边端拓扑——云端算力中心 → 前沿边缘节点 → 6 个执行集群（名称与四型数量同 T1-2）。
- 链路颜色语义与图例：稳定链路（绿）、波动链路（黄）、受限链路（红）；图中边缘节点↔集群 2 为黄色虚线（波动）、↔集群 6 为红色虚线（受限），其余为绿色。
- 左侧四项指标：链路质量指标、边缘节点负载、数据回传延迟（附图内波形）、本地缓存状态（含「可用 / 总量」）。
- 右侧：「云边端拓扑已构建」；云端连接（已连接）、边缘节点（在线）、执行集群（数）、链路稳定度（环形进度）。
- 底部：【进入下一步】、【链路优化】。

**T2-2 边缘自治能力激活界面**：
- 顶部四条对话行：
  - AI 提示：“云端策略模型已下发，边缘节点完成本地模型加载，具备自主决策能力。”
  - 系统语音：“当前系统支持云端全局优化 + 边缘实时决策 + 前端自主执行协同模式。”
  - 用户语音：“是否具备断链自主运行能力？”
  - 系统回复：“边缘节点已具备局部闭环运行能力，在通信波动条件下可维持任务连续执行。”
- 中央三段式能力示意：云端（全局策略优化）→ 边缘节点（实时推理与自主决策，标签「具备局部自治能力」）→ 执行单元（自主执行与反馈）；节点间四条箭头标注：策略下发 / 状态回传 / 任务指令 / 执行反馈。
- 右侧「能力状态」：云边协同（已启用）、边缘推理能力（已激活）、任务缓存机制（运行中），各带副说明；「自主运行条件」（已满足，可在断链条件下自主运行）。
- 底部：【进入侦察阶段】、【返回拓扑视图】。

### T3｜多源侦察与态势构建

**T3-1 多集群侦察展开界面**：
- 中央：地图上 6 个集群图标（编号 + 名称）各带发光同心圆与扫描扇面，集群间以多色虚线相连；左下角图例：光电侦察 / 雷达扫描 / 电子侦测 / 通信中继。
- 左侧「侦察资源」：6 个集群列表项，各显示在线状态。
- 右侧「侦查发现」：区域覆盖（环形进度）、已标记热点（数）、数据回传（正常）、多源侦察（进行中）；「侦察图例」4 行（光电图像 / 雷达扫描 / 电子侦测 / 通信中继，各带线型样例）。
- 底部：【进入态势融合】、【查看侦查详情】。

**T3-2 边缘融合态势界面**：
- 顶部 AI 提示横条：“边缘节点已完成第一轮态势融合，目标活动区域已完成初筛标记。”
- 中央「边缘融合态势」地图：6 个集群各带职能副标题——① 前出侦察集群（前沿侦察，目标发现）② 侧向感知集群（侧向覆盖，补盲感知）③ 雷达探测集群（广域探测，跟踪定位）④ 边缘协同处理集群（数据融合，本地处理）⑤ 电子对抗集群（信号干扰，电磁压制）⑥ 机动执行集群（快速机动，任务执行）；中央为多层同心圆融合效果，多根虚线向中心汇聚，含红色方框目标活动区标记。
- 底部系统语音横条：“多源信息已在边缘侧完成初步关联处理，并回传云端进行全局一致性校核。”
- 左侧「实时图像流」：3 路缩略图（前出侦察集群 / 侧向感知集群 / 雷达探测集群，各标在线）；「链路状态」：光电、雷达、电子、通信均在线。
- 右侧「边缘融合结果」：初步融合完成、局部态势已形成、疑似目标区已锁定、数据进入边云同步阶段、目标候选（3 处，待进一步分析确认）。
- 底部：【进入目标分析】、【查看融合详情】。

### T4｜目标识别、评估与任务生成

**T4-1 目标识别列表界面**：
- 顶部系统提示横条：“云端模型完成二次融合校核，生成高置信目标集合。”
- 中央地图：5 个编号目标标记（红/蓝分色，红为高威胁）、红色虚线关联连线、集群位置编号 1–6。
- 左侧「目标列表」：表头为 目标ID / 目标类型 / 威胁等级 / 来源 / 动态状态；目标类型含机动指挥节点、通信枢纽节点、防空火力单元；来源含融合、雷达、光电、电子；动态状态含机动中、活跃、部署中、转移中、短暂停留；面板底部显示目标总数与「全部目标」下拉筛选。
- 右侧「识别结果」：高置信目标数、高价值目标数、二次融合校核（已完成）、目标状态（持续更新）；「来源图例（可筛选）」：光电 / 雷达 / 电子 / 融合。
- 底部状态条（本场景唯一）：目标总数、高价值目标、威胁等级（高）、云端模型（运行正常）、数据链路（稳定）、更新时间。
- 底部按钮：【查看目标详情】、【进入任务生成】。

**T4-2 目标决策分析界面**：
- 中央「目标位置与轨迹回溯（目标 003）」：红色轨迹线自起点延伸至锁定标记，标注目标编号与类型；下方「轨迹回溯」时间轴（-02:00 / -01:00 / 00:00 现在 / +01:00 / +02:00）含游标与后退 / 播放 / 前进控制。
- 左侧「目标列表」：表头为 目标编号 / 目标类型 / 威胁等级 / 来源 / 动态状态，含筛选图标；选中行红色描边高亮，面板底部显示目标总数。
- 右侧四块：
  - 用户语音：“优先处理哪个目标？”
  - 系统语音：“目标003为关键协同节点，具备通信与指挥双重功能，建议优先纳入集群协同攻击序列。”
  - AI 分析输出：目标活动规律、暴露窗口预测（预计时间后出现暴露窗口）、机动路径推演（预测可能机动路径条数）、威胁等级评估（星级）。
  - 系统提示：云端评估完成、边缘节点已接收任务优先级、执行序列已生成。
- 底部：【加入攻击序列】（红色主按钮）、【持续跟踪】。
- 注：本场景 T4-1 / T4-2 / T5-2 / T7-2 的目标总数与同名目标类型不一致（演示数据，非需求）。

### T5｜任务决策与打击准备

**T5-1 AI 决策界面**：
- 顶部三条对话行：系统语音（已生成多种任务执行方案，可由边缘节点自主执行并实时回传云端优化结果）→ 用户语音“选择最优方案” → 系统回复（已选择方案二：集群协同攻击）。
- 中央三张方案卡（各含示意图、要点、预期效果星级）：
  1. 方案一｜光电精确打击（局部执行）：精准锁定单一高价值目标 / 以光电侦察引导精确打击 / 适用于局部小规模目标打击。
  2. 方案二｜集群协同攻击（集群协同）【AI推荐】：多集群协同、分布式自主执行 / 边缘自主决策、云端优化支撑 / 动态适配多目标与复杂战场环境。
  3. 方案三｜电子压制协同（电子压制）：电子压制与打击协同配合 / 干扰敌方通信与防空系统 / 适用于高防护目标区域。
- 右侧三块：AI 决策摘要（已生成执行方案 3 套、推荐最优方案 方案二、边缘自主能力 可执行、云端优化支持 已启用）；云边端协同状态（云端算力中心 / 边缘指控单元 / 前端无人集群 均在线）；任务环境评估（目标复杂度、通信环境、电磁环境、时间敏感度）。
- 底部：【查看方案详情】、【自动优化】、【确认采用推荐方案】。

**T5-2 执行确认界面**：
- 中央地图：6 个集群标注、中央目标标记、各集群至目标的青色汇聚虚线；图例：侦察链路 / 打击链路 / 无人机集群 / 光电无人机；地图下方「任务分配情况」三段流程（侦察集群组数 → 机动执行集群架数 → 目标编号与类型）。
- 左侧「目标清单」：目标编号 + 名称 + 威胁标签，含选中高亮项；目标名称含指挥通信节点、装甲机动编队、炮兵火力阵地、防空火力单元、后勤运输车队、集结待机兵力。
- 右侧四块：时间窗口（最优打击时间）；云边端协同状态（云端算力中心、边缘指控单元、前端无人集群）；边缘自主执行节点（前沿指控节点、本地决策能力、缓存任务数量）；光电无人机（待命数量、任务载荷状态、续航时间）。
- 底部：【调整方案】、【生成执行任务】。

### T6｜协同执行与引导

**T6-1 执行态势界面**：
- 中央地图：左上角图例 6 项（无人机集群 / 目标位置 / 侦察轨迹 / 攻击轨迹 / 数据链路 / 边缘节点）；6 个集群标注、中央目标标记、红色虚线攻击走廊、底部「前沿指控节点」。
- 地图下方三块小面板：
  - 多源融合图层：光电图像 / 雷达回波 / 电子频谱三路。
  - 目标跟踪：轨迹折线 + 「预测轨迹」虚线标注。
  - 集群状态：光电无人机数量 +「协同进入执行链路」，另有任务进度环。
- 右侧四块：集群执行态势（侦察集群数、执行集群数）；目标锁定状态（目标编号 + 类型 + 威胁标签）；攻击时序调整（时钟图标 +「边缘节点自主调整中」+ 时间轴，含可拖动游标）；链路状态（云端链路 / 边缘链路 / 集群链路）。
- 本界面无底部按钮。

**T6-2 引导控制界面**：
- 中央地图：左上角图例 4 项（侦察集群 / 执行集群 / 数据链路 / 攻击路径）；5 个集群标记、中央目标标记、底部「前沿指控节点」；地图下方「实时回传画面」三路（光电图像含红色锁定十字框 / 雷达回波 / 电子频谱）。
- 左侧「目标追踪」：目标编号与类型、光电锁定画面缩略图、距离、状态（锁定跟踪）；「集群协同状态」：侦察集群、电子对抗集群、执行集群。
- 右侧四块：边缘节点决策结果（攻击方式、攻击时序、打击优先级、决策置信度）；云端策略同步（策略同步状态、全局一致性校核、策略修正次数）；目标位置修正（当前位置经纬度、位置修正频率、修正精度）；任务执行状态（执行阶段、链路延迟、任务进度条）。
- 底部：【确认执行同步】、【重新优化策略】。

### T7｜评估与任务结束

**T7-1 效果评估地图界面**：
- 中央地图：左上角图例 5 项（集群轨迹 / 光电无人机 / 目标位置 / 毁伤评估 / 数据链路）；多个目标标记分色表示毁伤程度；中央目标高亮；底部「前沿指控节点」向各目标辐射攻击轨迹虚线。
- 地图下方「目标处置状态」：逐目标缩略图卡 + 处置结果（已摧毁 / 重创 / 受损）。
- 右侧四块：目标毁伤评估（总体毁伤率环形进度 + 四类计数图例：被摧毁 / 重创 / 受损 / 完好）；残余威胁区域（缩略图 + 警示标记）；边缘节点回传状态（光电图像回传 / 雷达回波回传 / 电子信号回传，各带状态与进度条）；任务效果评估（链路稳定性 / 边缘执行率 / 协同效率 / 任务完成度四项环形指标）。
- 本界面无底部按钮。

**T7-2 任务总结界面**：
- 顶部横幅：任务完成标识 + 副文字（集群协同攻击闭环已完成）；右侧三项指标（目标处置 n/n、任务用时、集群协同效率）。
- 中部「任务区域态势回顾」：地图含多个目标标记分色（已摧毁 / 受损 / 未受损）+ 图例、前沿指控节点、攻击轨迹。
- 右侧三块：任务执行效能评估（边缘自主执行率、云端策略优化次数、平均决策响应时间、光电无人机命中率、链路稳定性）；资源消耗统计（四型无人机消耗架数 + 任务存活率）；任务报告导出（报告编号、生成时间 +【导出报告】）。
- 底部：【返回场景选择】、【导出任务报告】、【结束任务体系】。
- 注：本场景 T7 图中未出现语音行，亦无「集群回收轨迹 / 复核侦察航线 / 前沿节点准备脱离」（属场景一）。
## 9. 场景三：立体融合攻坚

> 推荐场景：建筑群区域，建筑内部空间复杂、多层结构及未知威胁目标，需战术小组与无人集群协同搜索处置。
> 界面证据：`需求图与描述/场景3/`（T0–T7 各 2 张）
> 说明：本章按界面图逐屏提取并逐字抄录图中字段。图中数值为演示数据。
> 全局版式：16 张图均含顶部状态栏与左侧功能导航（高亮项随阶段变化：T4-1/T4-2 高亮【任务】，T5-1 高亮【目标】，其余高亮【态势】）；**无右下角语音按钮**，语音以面板形式出现（T0-2 波形 + 麦克风按钮、T5-1 「AI语音」卡、T7-2 「系统语音」卡 + 喇叭按钮、T4-2 纯文字对话气泡）；底部状态条仅在 T2-2、T4-1、T6-2 出现。
> 本场景为**人装协同**：资源除四型无人机外含「战术小组」，中央为三维建筑模型并可分层展开与楼层切换。

### T0｜任务加载与场景选择

**T0-1 任务态势主界面**：
- 左侧三张卡：
  - 任务来源：任务编号、任务来源（前方指挥部）、任务类型（建筑搜索任务）、接收时间。
  - 建筑区域范围：区域面积、建筑数量、楼层范围、地下结构；带蓝色虚线多边形图标与【查看边界】按钮。
  - 战术小组状态：小组名称、人员数量、在线状态、当前位置、装备状态、任务状态。
- 中央：夜间航拍三维城市影像（写字楼群）；蓝色虚线多边形标出建筑区域边界；无人机图标（带垂直向下光柱）与人员图标散布。
- 右侧：
  - AI 任务分析：建筑复杂度（星级 + 等级）、风险等级（星级 + 等级）。
  - 体系接入状态：无人机集群、侦察系统、通信系统、指挥平台、火力支援（各显示接入状态）。
- 底部：三张场景入口卡（各带名称 + 一句说明 + 进入箭头）：敏捷拒止布控（快速布设防御，阻断敌方推进）/ 集群协同攻击（无人集群协同，精准打击目标）/ **立体融合攻坚**（人装协同搜索，立体攻坚处置，带【推荐】标签并高亮）。

**T0-2 场景确认界面**：
- 左侧「AI助手」对话卡：AI 助手播报（已完成建筑区域态势建模，检测到建筑内部空间复杂、多层结构及未知威胁目标，建议采用【立体融合攻坚】模式）；用户语音（“推荐任务模式”）；AI 助手回复（建议由战术小组与无人集群协同执行搜索任务，系统持续提供态势更新、路径规划和风险预警）；卡底为声波波形 + 麦克风按钮（语音输入）。
- 中央：方块化三维建筑群模型（亮蓝描边、发光底座）；下方「任务说明」面板含 AI 分析 3 条结论、建筑空间复杂度（星级 + 等级）、敌情分布预测（红黄热力圆盘 + 高/中/低/未知风险图例）、推荐搜索策略四步（立体侦察 → 重点突破 → 人装协同 → 持续监控，各带一句说明）。
- 右侧「模式推荐」：模式名 +【推荐】标签 + 推荐理由 5 条。
- 底部：【确认进入任务】、【重新选择】。

### T1｜外围部署与任务编组

**T1-1 任务编组界面**：
- 左侧「可用资源」四张卡（各带在线状态与缩略图）：战术小组（人数 12 人、电量、位置）、光电侦察无人机（数量、电量、位置）、雷达侦察无人机、电子侦察无人机。
- 中央「AI智能协同方案」三张方案卡（各含小地图缩略图 + 一句说明）：
  1. 方案一 · 外围侦察优先：以外围侦察和态势构建为主，循环扫描重点区域。
  2. 方案二 · 人装协同搜索【AI推荐】：战术小组与无人机协同推进，逐层搜索，实时共享态势。
  3. 方案三 · 重点目标突入：快速锁定重点目标，集中力量突入并实施打击。
- 右侧「AI推荐理由」5 条（标题 + 说明）：建筑空间复杂 / 目标位置未知 / 适合人与无人平台同步推进 / 可持续共享态势 / 可实时修正路线。
- 底部：【确认方案】、【自动优化】。

**T1-2 任务区域确认界面**：
- 中央「任务区域划分地图」：三维方块建筑群，按颜色划分区域并带边界虚线；含「重点目标区域」（准星标记）、「高危区域」（三角警示标记）、无人机与人员图标、指北针。
- 左侧「区域划分」四张卡（各带颜色标识），每卡显示 负责战术小组 / 负责无人集群（光电、雷达、电子数量）/ 协同关系：
  1. 外围搜索区（蓝）
  2. 建筑入口区（绿）
  3. 重点搜索区（橙黄）
  4. 高危区域（红，小组待命）
- 右侧「图例说明」9 项：四类区域色框 + 战术小组位置 + 无人集群位置 + 建筑入口 + 重点目标区域 + 高危区域。
- 底部：「协同关系总览」四段流程（外围侦察 → 安全进入 → 协同搜索 → 风险预警，各带状态词）；「编组概况」（战术小组组数、无人集群架数、在线状态、数据链路、协同等级）；居中【确认编组】。

### T2｜建筑空间感知

**T2-1 空间态势界面**（中央标题「建筑三维态势」）：
- 左侧「建筑结构树」：地上建筑（可展开折叠）→ 各楼层 → 各房间号（如 101 大厅 / 102 通道 / 103 设备间 / 104 楼梯间；201 办公区 / 202 走廊 / 203 指挥室 / 204 卫生间；301 办公区 / 302 会议室 / 303 储物间 / 304 楼梯间）；屋顶层、地下空间折叠项；当前楼层与房间可选中（高亮）。
- 中央：三维建筑模型分层展开（3F/2F/1F 层间可见内部隔间与陈设），模型上标注房间号；无人机与人员图标分布；左侧竖排楼层切换条（^ / 3F / 2F / 1F / B1 / v）；底部图例：战术小组 / 无人机 / 入口出口。
- 右侧：
  - 实时感知信息：已搜索区域、未知区域、危险区域（各带百分比与进度条）。
  - AI 提示：如「建筑三层东侧发现异常活动，建议优先侦察。」
- 本界面无底部按钮。

**T2-2 多源融合界面**（顶部标题「多源信息融合」）：
- 左上「光电侦察画面」+【实时】标识：建筑群影像，带倍率显示与放大（全屏）按钮。
- 中央三块：
  - 「雷达穿透结果」+【实时】：建筑剖面线框透视图，楼内人员标记，房间高亮；卡右侧竖排楼层切换（3F/2F/1F/B1）。
  - 「电子侦察结果」+【实时】：频谱波形，上方信号源图标标记，波形含尖峰；底部图例：通信信号 / Wi-Fi信号 / 设备信号 / 干扰信号。
  - 「融合分析态势」：三维分层建筑模型（含风险区高亮、人员、无人机），黄虚线圈定区域；图例：无人机位置 / 战术小组位置 / 人员热区 / 风险区域 / 已搜索区域 / 未知区域；楼层切换；指北针；底部提示条「多源信息已融合，持续更新中…」。
- 右侧：
  - 数据融合状态：融合进度（环形进度）、融合质量、更新时间。
  - AI 融合提示：与 T2-1 同一提示（橙色警告框）。
  - 空间态势概览：已搜索区域、未知区域、风险区域（百分比 + 进度条）；人员热区数量、高风险区域数量。
- 底部状态条（本图存在）：数据源接入（n/n 在线）、无人机数量（在线）、战术小组（组数 在线）、数据融合延迟、融合态势可信度（星级 + 等级）。

### T3｜目标搜索

**T3-1 目标发现界面**：
- 中央：三维分层建筑模型（楼层展开），标注房间号；红色圆环准星标记「疑似目标位置」；红色虚线折线为目标移动轨迹；绿色人形为战术小组；无人机图标带光柱；左下角图例（无人机位置 / 战术小组位置 / 疑似目标位置 / 目标移动轨迹）；指北针与楼层切换。
- 右侧：
  - AI 提示：如「建筑二层发现疑似武装人员活动，建议优先侦察目标区域。」
  - 目标信息：目标编号、目标类型、目标位置（楼层 + 房间号）、目标来源、目标置信度（进度条）。
  - 目标动态：移动轨迹（折线图）、活动趋势。
  - 威胁评估：威胁等级（星级）、威胁类型。
- 底部工具按钮 4 个：【楼层透视】、【热成像】、【轨迹回放】、【视角切换】。

**T3-2 目标详情界面**：
- 中央：同一建筑模型与同一目标（对比 T3-1，置信度与威胁星级提升）；除红色目标移动轨迹外，新增绿色虚线路径（战术小组至目标）；图例含目标位置 / 目标移动轨迹 / 战术小组位置 / 无人机位置。
- 右侧：
  - 目标详情：目标编号、目标类型、人数估计、威胁等级（星级）、置信度（进度条）。
  - 目标特征：3 条（如轻型武器携带 / 集中活动区域 / 室内移动频繁）。
  - 目标来源：来源平台 + 实时侦察状态。
  - 动作按钮：【持续观察】、【加入行动路线】。
- 底部工具按钮 2 个：【视角切换】、【时间回放】。

### T4｜人装协同引导

**T4-1 协同行动界面**（左导航高亮【任务】）：
- 左侧：
  - 人员状态：小组名称、在线人数（如 8 / 12）、当前位置、行动状态。
  - 装备状态：光电无人机、雷达无人机、电子无人机（各显示电量与在线状态）。
- 中央：夜间航拍三维城市；蓝色发光推荐路线自下方（战术小组标记）延伸至「目标区域」标记；路线中部标注入口名称；两侧红色菱形虚线框 + 三角警示（高危区域）；无人机图标带光柱。
- 右侧：AI 提示（引文形式，如“已为战术小组规划最优搜索路线，无人平台将在前方持续搜索并同步预警。”）；预计接敌时间（大字）；风险等级（圆环仪表）；推荐行动方向。
- 底部：【确认路线】、【继续推进】；状态条（本图存在）：任务、天气、风速、可见度、通信链路。

**T4-2 路线调整界面**（左导航高亮【任务】）：
- 中央顶部图例：战术小组路线（实线）/ 无人机路线（虚线）/ 火力覆盖区域（斜纹方块）/ 目标位置（准星）。
- 中央地图：蓝色实线路线自入口绕行至目标位置；青色虚线无人机路线；两处「火力覆盖区」斜纹区域（各带三角警示）；路线节点标记；另一入口标注。
- 左侧：
  - 对话交互：用户语音“重新规划路线”；AI 助手回复“已避开火力覆盖区域，生成新的协同行动路线。”
  - 路线概要：协同状态、避让区域（处数）、预计用时、队形同步。
- 右侧：
  - AI 路线指引：风险等级（星级 + 等级）、推荐方向、协同进度（进度条）、接敌预计。
  - 协同时间轴：四节点（入口接近 / 外围侦察 / 协同推进 / 目标接近），各带预计到达或完成时间。
- 底部：【确认路线】、【继续推进】。

### T5｜协同处置

**T5-1 处置界面**（左导航高亮【目标】）：
- 左侧「任务目标」：目标编号、目标类型、活动状态、置信度。
- 中央：夜间航拍三维城市；建筑红色高亮框选 + 准星目标标记；绿色高亮带 + 盾牌图标「安全通道」；战术小组人员图标；无人机图标带光柱；蓝色虚线路线穿过安全通道；画中画「无人机观测」（含【在线】标识、取景框、倍率与放大）；
- 中央下方「AI语音」卡：文本如「目标已进入处置窗口，无人平台持续监视周边区域。」+ 声波波形。
- 右侧：
  - 新增威胁提示：方位与距离描述、威胁等级（星级）。
  - 目标变化：活动状态、移动趋势（方向箭头）。
  - 人员距离：与目标距离（大字）、风险等级（刻度点 + 等级）。
- 底部：【开始处置】、【继续观察】。

**T5-2 行动协同界面**（左导航高亮【态势】）：
- 左侧：
  - 战术小组：小组名与角色、在线状态、人员数、位置、状态、装备状态 + 缩略图。
  - 前出无人机：各无人机编号、在线状态、位置、电量（电池条）+ 缩略图。
  - 协同状态：人机协同、通信链路、任务进度（进度条）、预计剩余时间。
- 中央：三维方块建筑模型；标注侦察无人机位置与航线；红色高亮区 + 准星「目标 A」+ 三角「高风险区」；橙黄色区 + 准星「目标 B」+ 三角「潜在威胁」；绿色高亮区 + 门形图标「突击入口」；人员图标沿路线推进；画中画「无人机实时画面」（带【实时】标识、信号格、放大按钮）。
- 右侧：
  - 风险预警：高风险区域（位置）、潜在威胁（位置）、环境风险。
  - 目标状态：目标A（主要目标）状态与可信度；目标B（次要目标）状态与可信度。
  - 协同状态：人员状态、无人机状态、数据共享、指挥链路。
- 底部：【协同推进】、【继续监视】。

### T6｜区域清剿

**T6-1 区域搜索界面**：
- 左侧：
  - 搜索状态：已完成区域 / 复查区域 / 未搜索区域（各带百分比与负责单位）。
  - 区域概览：搜索进度（环形进度 + 已完成百分比）、已识别入口数量、活跃单位（组数 / 人数）、活跃无人机架数。
- 中央：三层建筑分层展开模型，楼层按状态着色（绿=已完成 / 橙黄=复查 / 红=未搜索）；无人机与人员图标；入口标注；「地下层」标注；外围蓝色虚线边界。
- 右侧：图例说明（三类区域色块 + 战术小组 + 无人机 + 建筑入口，各带含义）；AI 提示（如“建议继续搜索地下空间。”）。
- 底部：【继续搜索】、【调整搜索策略】。

**T6-2 残余威胁分析界面**（中央标题「残余威胁分析」）：
- 中央：三维分层建筑模型，绿色区（已清理）、橙黄色区（可疑，带三角警示）、红色圆环准星（残余目标）；指北针；底部图例：残余目标 / 可疑区域 / 已清理区域；楼层切换。
- 右侧：
  - AI 分析 → 残余威胁分布列表：各条目为位置 + 类型标记（残余目标 / 可疑区域）。
  - 风险等级：等级文字 + 渐变风险刻度条（含指针位置）。
  - AI 提示：如“发现地下空间与二层西侧仍存在可疑活动，建议按优先顺序继续搜索。”
- 底部 4 张指标卡：残余目标数、可疑区域数、危险等级、建议搜索顺序（顺序链）；按钮【继续搜索】、【完成区域控制】；状态条（本图存在）：数据链路、无人集群、区域评估。

### T7｜态势更新与任务结束

**T7-1 建筑安全态势界面**：
- 左侧色块图例：已控制 / 持续监控 / 残余风险。
- 中央：三维建筑群按状态着色（绿=已控制，带盾牌标记；琥珀=持续监控，带眼睛标记；红=残余风险，带三角警示）；多处「无人机返航」标注（图标 + 弧形返航虚线路径）与「战术小组撤离」标注（奔跑人形 + 撤离虚线路径）。
- 右侧：
  - 区域状态：环形图 + 三行（已控制 / 持续监控 / 残余风险，各带百分比与栋数）。
  - 无人机状态：返航中架数、已返航架数。
  - 战术小组状态：撤离中组数、安全组数。
  - AI 提示：如“重点区域建议持续监视，保持空中巡查。”
- 底部 4 张指标卡：搜索完成率（含已执行栋数）、建筑控制范围（含已控制栋数）、风险目标数、数据链路（含延迟）。
- 本界面无底部按钮。

**T7-2 任务总结界面**：
- 左侧：
  - 系统语音卡：语音文本（建筑搜索任务完成，系统已完成区域安全评估，建议保持重点区域监视）+ 声波波形 + 喇叭按钮。
  - 任务信息：任务编号、任务类型、执行时长、开始时间、完成时间、任务状态、区域安全评估。
  - 底部横幅：任务已顺利完成。
- 中央：
  - 任务结果概览：三维建筑群 + 安全标识 + 人员与无人机图标 + 边界。
  - 区域态势 4 小块：整体态势、人员状态、无人平台、通信链路。
  - 建筑控制地图（二维俯视）：图例为 已控制 / 重点监视 / 未进入 / 无建筑区域（颜色区分）。
- 右侧「AI报告」（每项带进度条）：搜索覆盖率、目标发现数量、人员行动轨迹、无人平台协同效率、风险预警次数、建筑控制范围、体系协同效率；底部结论框（区域安全可控 + 建议保持重点区域监视）。
- 底部：【返回场景选择】、【导出任务报告】、【结束任务】。
## 10. 通用功能与组件清单

> 本节按三个场景 54 张界面图汇总实际出现的功能与组件。

### 10.1 地图与态势
- 三维地形态势地图（场景一/二）/ 三维建筑群态势地图（场景三）
- 2D/3D 切换、图层控制、测距、区域选择、新建、全屏、标绘
- 显示模式切换（综合态势、侦察展开、侦察融合、目标识别、任务规划、打击确认、实时态势、引导控制、复核态势、结果汇总）
- 任务区域多边形标注与颜色分区（A/B/C 区域、搜索区、重点侦察区、高危区）
- 集群轨迹动画、目标锁定框、热点标记、预测轨迹、已覆盖区域边界
- 威胁等级热力图、多源数据融合图层、毁伤/控制状态着色
- 比例尺、指北针、缩放、楼层切换条、分层展开与结构树（场景三）

### 10.2 AI 与语音
- AI 任务分析卡片（风险等级、威胁强度、态势简述、建议方向）
- AI 推荐方案高亮、推荐评分/星级、推荐理由
- AI 提示卡、AI 分析结果、AI 确认建议、AI 摘要
- AI 语音提示与自动播报、系统语音、用户语音指令识别、系统回复
- 语音呈现两种形态：右下角麦克风按钮（场景一）／页面内对话行与语音卡（场景二、三）

### 10.3 资源与编组
- 无人机资源列表（光电、雷达、电子、通信四型，含总数/可用数/分配数与状态）
- 战术小组资源卡（场景三：人数、电量、位置、状态）
- 集群编组方案生成与对比（三套方案 + 逐集群四型配比）
- 自动优化、确认采用推荐方案、确认编组

### 10.4 通信与链路
- 通信拓扑图 / 云边端拓扑图（云端算力中心 → 前沿边缘节点 → 执行集群）
- 链路质量指标（信号强度、带宽、时延、丢包率、覆盖范围、局部组网进度）
- 链路状态颜色编码（绿=稳定/已连接，黄=波动/弱链路，红=受限）
- 边缘节点负载、数据回传延迟、本地缓存状态、边缘自治与任务缓存

### 10.5 目标管理
- 目标列表（编号、类型、威胁等级、来源、动态状态、坐标、置信度）
- 高价值/高威胁目标红色标记、目标筛选（类型/来源/状态下拉）
- 目标详情与 AI 分析、轨迹回溯时间轴与回放控制（场景二）
- 人员热区、目标特征、人数估计、移动趋势（场景三）
- 加入打击/攻击/行动序列、持续跟踪/持续观察

### 10.6 任务执行与评估
- 任务流程进度条与阶段时间轴（任务下达 → … → 任务完成）
- 方案卡片（打击/执行方案、成功率或星级、预期效果）
- 打击窗口时间轴与倒计时、攻击时序调整时间轴、协同时间轴
- 实时引导与偏差修正、命中概率、跟踪稳定度、任务进度环
- 结果指标卡与环形指标（覆盖率、协同效率、毁伤率、控制范围）
- 任务总结报告自动生成与导出（报告编号、生成时间）

---

## 11. 待确认问题清单

1. **业务目标**：该系统面向的具体用户角色是什么（指挥员、操作员、战术小组组长）？不同角色权限是否不同？
2. **数据口径**：
   - 无人机数量、在线率、资源利用率的计算规则。
   - 威胁等级、风险等级、成功率的评分标准。
   - 链路质量指标（带宽、延迟、丢包）的数据来源与刷新频率。
3. **AI 决策规则**：AI 推荐方案的核心算法/规则是什么？是否允许用户强制选择非推荐方案？
4. **语音交互**：支持的语音指令全集是什么？是否需要离线语音识别？
5. **地图数据**：三维地图数据来源（自研、第三方、离线/在线）？建筑模型精度要求？
6. **通信架构**：云边端与纯自组网两种模式的具体切换条件是什么？
7. **硬件适配**：是否仅支持平板横屏？是否需适配其他分辨率或设备？界面图存在三种画幅（约 1536×1024、1672×941、1448×1086），需明确设计基准分辨率。
8. **异常处理**：
   - 无人机失联、链路中断、边缘节点故障时的系统行为。
   - 自检不通过时的处理流程。
9. **输出与归档**：任务报告格式（PDF/Word/在线）？保存位置？是否需对接项目资料库？
10. **安全合规**：系统是否涉及真实地理/军事情报数据？是否需要国产化、保密或等保要求？
11. **全局框架一致性（据界面图实测）**：
    - 右下角语音按钮仅在场景一出现，场景二、三共 32 张图中均无，而语音入口是核心交互，需统一形态。
    - 底部全局状态条出现范围不一致：场景一 T0/T1 等有；场景二仅 T4-1；场景三仅 T2-2、T4-1、T6-2。
    - 启动阶段与两张独立态势图无左侧功能导航，是否属独立的另一类版式？
    - 两张独立态势图（任务执行态势、红方侦察态势）与平板端关系待定：是大屏展示、投屏输出，还是同一产品的另一视图？
12. **目标台账口径**：同一场景内目标编号与类型在不同界面不一致（如场景二 T4-1 为 5 个目标而 T4-2 为 6 个、T7-2 为 7 个；场景一 T4 高价值目标为 002 而文字稿为 003）。需明确目标是否以任务为维度唯一编号、跨界面是否强一致。
13. **三种交付形态的关系**：界面图包含平板端 T0–T7 流程界面、场景一的两张大屏态势图、场景三的二维建筑控制图，需明确各自在演示/实战中的角色与切换方式。

---

## 12. 下一步建议

1. 根据本初稿组织产品、设计、研发、测试评审，逐项确认“待确认问题清单”。
2. 将确认的 PRD 上传到项目资料库，并创建评审事项，分配给技术负责人、设计师、测试同学。
3. 针对每个 T 阶段补充详细的交互说明、状态清单、边界场景和验收标准。
4. 补充数据埋点需求，用于后续上线效果复盘。
5. 优先澄清第 11 章第 11–13 项（全局框架一致性、目标台账口径、交付形态关系）——这三项直接影响前端框架与数据结构设计，宜在技术需求文档定稿前确认。
